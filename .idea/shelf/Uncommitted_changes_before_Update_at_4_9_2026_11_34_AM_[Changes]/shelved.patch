Index: src/main_app/BetterAngle.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include <windows.h>\r\n#include <iostream>\r\n#include <fstream>\r\n#include <string>\r\n#include <vector>\r\n#include <thread>\r\n#include <atomic>\r\n#include <dwmapi.h>\r\n#include <gdiplus.h>\r\n\r\n#include \"shared/Input.h\"\r\n#include \"shared/Overlay.h\"\r\n#include \"shared/Logic.h\"\r\n#include \"shared/Detector.h\"\r\n#include \"shared/Updater.h\"\r\n#include \"shared/Profile.h\"\r\n#include \"shared/Tray.h\"\r\n#include \"shared/Remote.h\"\r\n#include \"shared/Startup.h\"\r\n#include \"shared/ControlPanel.h\"\r\n\r\n#pragma comment(lib, \"dwmapi.lib\")\r\n#pragma comment(lib, \"gdiplus.lib\")\r\n\r\nusing namespace Gdiplus;\r\n\r\n#include \"shared/State.h\"\r\n\r\n// Global State\r\nHWND g_hHUD = NULL;\r\nHWND g_hPanel = NULL;\r\nULONG_PTR g_gdiplusToken;\r\nstd::atomic<bool> g_running(true);\r\nFovDetector g_detector;\r\n\r\nProfile g_currentProfile;\r\nstd::vector<Profile> g_allProfiles;\r\nint g_selectedProfileIdx = 0;\r\n\r\n// FOV Detector Thread\r\nvoid DetectorThread() {\r\n    while (g_running) {\r\n        if (!g_allProfiles.empty() && g_currentSelection == NONE) {\r\n            Profile& p = g_allProfiles[g_selectedProfileIdx];\r\n            RoiConfig cfg = { p.roi_x, p.roi_y, p.roi_w, p.roi_h, p.target_color, p.tolerance };\r\n            \r\n            g_detectionRatio = g_detector.Scan(cfg);\r\n            if (g_detectionRatio > 0.05f) {\r\n                g_logic.SetScale(p.scale_diving);\r\n            } else {\r\n                g_logic.SetScale(p.scale_normal);\r\n            }\r\n        }\r\n        std::this_thread::sleep_for(std::chrono::milliseconds(100));\r\n    }\r\n}\r\n\r\n// Screen Snapshot for Flicker-Free Selection (v4.9.15)\r\nvoid CaptureDesktop() {\r\n    int sw = GetSystemMetrics(SM_CXSCREEN);\r\n    int sh = GetSystemMetrics(SM_CYSCREEN);\r\n    HDC hdcScreen = GetDC(NULL);\r\n    HDC hdcMem = CreateCompatibleDC(hdcScreen);\r\n    if (g_screenSnapshot) DeleteObject(g_screenSnapshot);\r\n    g_screenSnapshot = CreateCompatibleBitmap(hdcScreen, sw, sh);\r\n    HGDIOBJ hOld = SelectObject(hdcMem, g_screenSnapshot);\r\n    BitBlt(hdcMem, 0, 0, sw, sh, hdcScreen, 0, 0, SRCCOPY);\r\n    SelectObject(hdcMem, hOld);\r\n    ReleaseDC(NULL, hdcScreen);\r\n    DeleteDC(hdcMem);\r\n}\r\n\r\n// HUD Window Procedure\r\nLRESULT CALLBACK HUDWndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam) {\r\n    switch (message) {\r\n        case WM_CREATE:\r\n            SetLayeredWindowAttributes(hWnd, RGB(0, 0, 0), 0, LWA_COLORKEY);\r\n            RegisterRawMouse(hWnd);\r\n            RegisterHotKey(hWnd, 1, MOD_CONTROL, 'U'); // Toggle Panel\r\n            RegisterHotKey(hWnd, 2, MOD_CONTROL, 'R'); // ROI Select\r\n            RegisterHotKey(hWnd, 3, 0, VK_F10);        // Crosshair\r\n            RegisterHotKey(hWnd, 4, MOD_CONTROL, 'G'); // Zero Angle\r\n            RegisterHotKey(hWnd, 5, MOD_CONTROL, '9'); // Secret Debug\r\n            return 0;\r\n\r\n        case WM_HOTKEY:\r\n            switch (wParam)\r\n            {\r\n                case 1: // Toggle Panel\r\n                    if (IsIconic(g_hPanel)) ShowWindow(g_hPanel, SW_RESTORE);\r\n                    else if (IsWindowVisible(g_hPanel)) ShowWindow(g_hPanel, SW_MINIMIZE);\r\n                    else ShowWindow(g_hPanel, SW_SHOW);\r\n                    break;\r\n                case 2: // ROI Select Toggle\r\n                    if (g_currentSelection == NONE) {\r\n                        CaptureDesktop(); // Capture before dimming\r\n                        g_currentSelection = SELECTING_ROI;\r\n                        g_isSelectionActive = true;\r\n                        long exStyle = GetWindowLong(hWnd, GWL_EXSTYLE);\r\n                        exStyle &= ~WS_EX_TRANSPARENT;\r\n                        SetWindowLong(hWnd, GWL_EXSTYLE, exStyle);\r\n                        SetForegroundWindow(hWnd);\r\n                    } else {\r\n                        g_currentSelection = NONE;\r\n                        g_isSelectionActive = false;\r\n                        SetWindowLong(hWnd, GWL_EXSTYLE, GetWindowLong(hWnd, GWL_EXSTYLE) | WS_EX_TRANSPARENT);\r\n                    }\r\n                    break;\r\n                case 3:\r\n                    g_showCrosshair = !g_showCrosshair;\r\n                    break;\r\n        case 4:\r\n            g_currentAngle = 0.0f;\r\n            break;\r\n        case 5:\r\n            g_debugMode = !g_debugMode;\r\n            break;\r\n    }\r\n    return 0;\r\n        case WM_LBUTTONDOWN:\r\n            if (g_currentSelection == SELECTING_ROI) {\r\n                POINT cur; GetCursorPos(&cur);\r\n                g_startPoint = cur;\r\n                g_selectionRect = { cur.x, cur.y, cur.x, cur.y };\r\n            } else if (g_currentSelection == SELECTING_COLOR) {\r\n                // STAGE 2: PRECISION COLOR PICK (Snap-Shot Bypass)\r\n                if (g_screenSnapshot) {\r\n                    HDC hdcScreen = GetDC(NULL);\r\n                    HDC hdcMem = CreateCompatibleDC(hdcScreen);\r\n                    HGDIOBJ hOld = SelectObject(hdcMem, g_screenSnapshot);\r\n                    \r\n                    POINT cur; GetCursorPos(&cur);\r\n                    COLORREF pixel = GetPixel(hdcMem, cur.x, cur.y);\r\n                    // Sync: GDI returns 0x00BBGGRR\r\n                    g_targetColor = pixel; \r\n                    g_pickedColor = pixel;\r\n\r\n                    SelectObject(hdcMem, hOld);\r\n                    DeleteDC(hdcMem);\r\n                    ReleaseDC(NULL, hdcScreen);\r\n                }\r\n\r\n                // Finalize and Exit Selection\r\n                g_currentSelection = NONE;\r\n                g_isSelectionActive = false;\r\n                SetWindowLong(hWnd, GWL_EXSTYLE, GetWindowLong(hWnd, GWL_EXSTYLE) | WS_EX_TRANSPARENT);\r\n\r\n                if (!g_allProfiles.empty()) {\r\n                    Profile& p = g_allProfiles[g_selectedProfileIdx];\r\n                    p.target_color = g_targetColor;\r\n                    p.roi_x = min(g_selectionRect.left, g_selectionRect.right);\r\n                    p.roi_y = min(g_selectionRect.top, g_selectionRect.bottom);\r\n                    p.roi_w = abs(g_selectionRect.right - g_selectionRect.left);\r\n                    p.roi_h = abs(g_selectionRect.bottom - g_selectionRect.top);\r\n                    \r\n                    // Save to the actual profile path\r\n                    std::wstring profilePath = L\"profiles/\" + p.name + L\".json\";\r\n                    p.Save(profilePath);\r\n                    \r\n                    // Also maintain the legacy 'last_calibrated' for quick-load logic if needed\r\n                    p.Save(L\"profiles/last_calibrated.json\");\r\n                }\r\n            }\r\n            return 0;\r\n\r\n        case WM_MOUSEMOVE:\r\n            if (g_currentSelection != NONE) {\r\n                if (g_currentSelection == SELECTING_ROI && (wParam & MK_LBUTTON)) {\r\n                    POINT cur; GetCursorPos(&cur);\r\n                    g_selectionRect.right = cur.x;\r\n                    g_selectionRect.bottom = cur.y;\r\n                }\r\n                InvalidateRect(hWnd, NULL, FALSE);\r\n            }\r\n            return 0;\r\n\r\n        case WM_LBUTTONUP:\r\n            if (g_currentSelection == SELECTING_ROI) {\r\n                g_currentSelection = SELECTING_COLOR;\r\n                InvalidateRect(hWnd, NULL, FALSE);\r\n            }\r\n            return 0;\r\n\r\n\t\tcase WM_INPUT: {\r\n            // THE FIX: Send the mouse delta to the logic engine\r\n            int dx = GetRawInputDeltaX(lParam);\r\n            g_logic.Update(dx);\r\n            return 0;\r\n        }\r\n\r\n        case WM_TIMER: {\r\n            CURSORINFO ci = { sizeof(CURSORINFO) };\r\n            if (GetCursorInfo(&ci)) {\r\n                g_isCursorVisible = (ci.flags & CURSOR_SHOWING);\r\n            }\r\n            InvalidateRect(hWnd, NULL, FALSE);\r\n            return 0;\r\n        }\r\n\r\n        case WM_PAINT:\r\n            DrawOverlay(hWnd, g_logic.GetAngle(), g_status.c_str(), g_detectionRatio, g_showCrosshair);\r\n            return 0;\r\n\r\n        case WM_DESTROY:\r\n            g_running = false;\r\n            PostQuitMessage(0);\r\n            return 0;\r\n\r\n        default:\r\n            return DefWindowProc(hWnd, message, wParam, lParam);\r\n    }\r\n}\r\n\r\nint WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow) {\r\n    // Phase 0: Silent Update & Relaunch Sequence\r\n    if (CheckForUpdates()) {\r\n        std::wstring verStr = std::wstring(g_latestVersionOnline.begin(), g_latestVersionOnline.end());\r\n        // User requested: Download the latest .exe from GitHub to update_tmp.exe\r\n        std::wstring downloadUrl = L\"https://github.com/MahanYTT/BetterAngle/releases/latest/download/BetterAngle.exe\";\r\n        if (DownloadUpdate(downloadUrl, L\"update_tmp.exe\")) {\r\n            ApplyUpdateAndRestart();\r\n        }\r\n    }\r\n\r\n    GdiplusStartupInput gdiplusStartupInput;\r\n    GdiplusStartup(&g_gdiplusToken, &gdiplusStartupInput, NULL);\r\n\r\n    // Phase 1: Startup Sequence (Splash)\r\n    ShowSplashLoader(hInstance);\r\n\r\n    // Initial Load (Syncing was handled or represented in Splash)\r\n    g_allProfiles = GetProfiles(L\"profiles\");\r\n    if (!g_allProfiles.empty()) {\r\n        g_currentProfile = g_allProfiles[0];\r\n        g_logic.SetScale(g_currentProfile.scale_normal);\r\n    }\r\n\r\n    // Phase 2: Create Control Panel (Interactive)\r\n    g_hPanel = CreateControlPanel(hInstance);\r\n    \r\n    // Phase 3: Create HUD Window (Transparent Overlay)\r\n    WNDCLASS wc = { 0 };\r\n    wc.lpfnWndProc = HUDWndProc;\r\n    wc.hInstance = hInstance;\r\n    wc.hCursor = LoadCursor(NULL, IDC_ARROW);\r\n    wc.lpszClassName = L\"BetterAngleHUD\";\r\n    RegisterClass(&wc);\r\n\r\n    int screenW = GetSystemMetrics(SM_CXSCREEN);\r\n    int screenH = GetSystemMetrics(SM_CYSCREEN);\r\n\r\n    g_hHUD = CreateWindowEx(\r\n        WS_EX_TOPMOST | WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_TOOLWINDOW,\r\n        L\"BetterAngleHUD\", L\"BetterAngle HUD\",\r\n        WS_POPUP,\r\n        0, 0, screenW, screenH,\r\n        NULL, NULL, hInstance, NULL\r\n    );\r\n    RegisterRawMouse(g_hHUD);\r\n    ShowWindow(g_hHUD, SW_SHOW);\r\n    UpdateWindow(g_hHUD);\r\n    SetTimer(g_hHUD, 1, 25, NULL);\r\n\r\n    std::thread detThread(DetectorThread);\r\n\r\n    MSG msg;\r\n    while (GetMessage(&msg, NULL, 0, 0)) {\r\n        TranslateMessage(&msg);\r\n        DispatchMessage(&msg);\r\n        \r\n        // If Panel is closed by user (WM_QUIT from Panel should not happen but WM_DESTROY does)\r\n        if (!IsWindow(g_hPanel) && !IsWindow(g_hHUD)) break;\r\n    }\r\n\r\n    g_running = false;\r\n    if (detThread.joinable()) detThread.join();\r\n    GdiplusShutdown(g_gdiplusToken);\r\n    return (int)msg.wParam;\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main_app/BetterAngle.cpp b/src/main_app/BetterAngle.cpp
--- a/src/main_app/BetterAngle.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/main_app/BetterAngle.cpp	(date 1775748098233)
@@ -1,23 +1,23 @@
-#include <windows.h>
-#include <iostream>
-#include <fstream>
-#include <string>
-#include <vector>
-#include <thread>
 #include <atomic>
 #include <dwmapi.h>
+#include <fstream>
 #include <gdiplus.h>
+#include <iostream>
+#include <string>
+#include <thread>
+#include <vector>
+#include <windows.h>
 
+#include "shared/ControlPanel.h"
+#include "shared/Detector.h"
 #include "shared/Input.h"
-#include "shared/Overlay.h"
 #include "shared/Logic.h"
-#include "shared/Detector.h"
-#include "shared/Updater.h"
+#include "shared/Overlay.h"
 #include "shared/Profile.h"
-#include "shared/Tray.h"
 #include "shared/Remote.h"
 #include "shared/Startup.h"
-#include "shared/ControlPanel.h"
+#include "shared/Tray.h"
+#include "shared/Updater.h"
 
 #pragma comment(lib, "dwmapi.lib")
 #pragma comment(lib, "gdiplus.lib")
@@ -29,7 +29,7 @@
 // Global State
 HWND g_hHUD = NULL;
 HWND g_hPanel = NULL;
-ULONG_PTR g_gdiplusToken;
+ULONG_PTR g_gdiplusToken = 0;
 std::atomic<bool> g_running(true);
 FovDetector g_detector;
 
@@ -39,241 +39,403 @@
 
 // FOV Detector Thread
 void DetectorThread() {
-    while (g_running) {
-        if (!g_allProfiles.empty() && g_currentSelection == NONE) {
-            Profile& p = g_allProfiles[g_selectedProfileIdx];
-            RoiConfig cfg = { p.roi_x, p.roi_y, p.roi_w, p.roi_h, p.target_color, p.tolerance };
-            
-            g_detectionRatio = g_detector.Scan(cfg);
-            if (g_detectionRatio > 0.05f) {
-                g_logic.SetScale(p.scale_diving);
-            } else {
-                g_logic.SetScale(p.scale_normal);
-            }
-        }
-        std::this_thread::sleep_for(std::chrono::milliseconds(100));
-    }
+  while (g_running) {
+    {
+      std::lock_guard<std::mutex> lock(g_stateMutex);
+      if (!g_allProfiles.empty() && g_currentSelection == NONE) {
+        Profile &p = g_allProfiles[g_selectedProfileIdx];
+        RoiConfig cfg = {p.roi_x, p.roi_y,        p.roi_w,
+                         p.roi_h, p.target_color, p.tolerance};
+
+        g_detectionRatio = g_detector.Scan(cfg);
+        if (g_detectionRatio > 0.05f) {
+          g_logic.SetScale(p.scale_diving);
+        } else {
+          g_logic.SetScale(p.scale_normal);
+        }
+      }
+    }
+    std::this_thread::sleep_for(std::chrono::milliseconds(100));
+  }
 }
 
 // Screen Snapshot for Flicker-Free Selection (v4.9.15)
 void CaptureDesktop() {
-    int sw = GetSystemMetrics(SM_CXSCREEN);
-    int sh = GetSystemMetrics(SM_CYSCREEN);
-    HDC hdcScreen = GetDC(NULL);
-    HDC hdcMem = CreateCompatibleDC(hdcScreen);
-    if (g_screenSnapshot) DeleteObject(g_screenSnapshot);
-    g_screenSnapshot = CreateCompatibleBitmap(hdcScreen, sw, sh);
-    HGDIOBJ hOld = SelectObject(hdcMem, g_screenSnapshot);
-    BitBlt(hdcMem, 0, 0, sw, sh, hdcScreen, 0, 0, SRCCOPY);
-    SelectObject(hdcMem, hOld);
-    ReleaseDC(NULL, hdcScreen);
-    DeleteDC(hdcMem);
+  std::lock_guard<std::mutex> lock(g_stateMutex);
+  int sw = GetSystemMetrics(SM_CXSCREEN);
+  int sh = GetSystemMetrics(SM_CYSCREEN);
+  HDC hdcScreen = GetDC(NULL);
+  if (!hdcScreen)
+    return;
+
+  HDC hdcMem = CreateCompatibleDC(hdcScreen);
+  if (!hdcMem) {
+    ReleaseDC(NULL, hdcScreen);
+    return;
+  }
+
+  if (g_screenSnapshot)
+    DeleteObject(g_screenSnapshot);
+  g_screenSnapshot = CreateCompatibleBitmap(hdcScreen, sw, sh);
+  if (!g_screenSnapshot) {
+    DeleteDC(hdcMem);
+    ReleaseDC(NULL, hdcScreen);
+    return;
+  }
+
+  HGDIOBJ hOld = SelectObject(hdcMem, g_screenSnapshot);
+  BitBlt(hdcMem, 0, 0, sw, sh, hdcScreen, 0, 0, SRCCOPY);
+  SelectObject(hdcMem, hOld);
+  DeleteDC(hdcMem);
+  ReleaseDC(NULL, hdcScreen);
 }
 
 // HUD Window Procedure
-LRESULT CALLBACK HUDWndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam) {
-    switch (message) {
-        case WM_CREATE:
-            SetLayeredWindowAttributes(hWnd, RGB(0, 0, 0), 0, LWA_COLORKEY);
-            RegisterRawMouse(hWnd);
-            RegisterHotKey(hWnd, 1, MOD_CONTROL, 'U'); // Toggle Panel
-            RegisterHotKey(hWnd, 2, MOD_CONTROL, 'R'); // ROI Select
-            RegisterHotKey(hWnd, 3, 0, VK_F10);        // Crosshair
-            RegisterHotKey(hWnd, 4, MOD_CONTROL, 'G'); // Zero Angle
-            RegisterHotKey(hWnd, 5, MOD_CONTROL, '9'); // Secret Debug
-            return 0;
+LRESULT CALLBACK HUDWndProc(HWND hWnd, UINT message, WPARAM wParam,
+                            LPARAM lParam) {
+  switch (message) {
+  case WM_CREATE:
+    SetLayeredWindowAttributes(hWnd, RGB(0, 0, 0), 0, LWA_COLORKEY);
+    RegisterRawMouse(hWnd);
+    RegisterHotKey(hWnd, 1, MOD_CONTROL, 'U'); // Toggle Panel
+    RegisterHotKey(hWnd, 2, MOD_CONTROL, 'R'); // ROI Select
+    RegisterHotKey(hWnd, 3, 0, VK_F10);        // Crosshair
+    RegisterHotKey(hWnd, 4, MOD_CONTROL, 'G'); // Zero Angle
+    RegisterHotKey(hWnd, 5, MOD_CONTROL, '9'); // Secret Debug
+    return 0;
 
-        case WM_HOTKEY:
-            switch (wParam)
-            {
-                case 1: // Toggle Panel
-                    if (IsIconic(g_hPanel)) ShowWindow(g_hPanel, SW_RESTORE);
-                    else if (IsWindowVisible(g_hPanel)) ShowWindow(g_hPanel, SW_MINIMIZE);
-                    else ShowWindow(g_hPanel, SW_SHOW);
-                    break;
-                case 2: // ROI Select Toggle
-                    if (g_currentSelection == NONE) {
-                        CaptureDesktop(); // Capture before dimming
-                        g_currentSelection = SELECTING_ROI;
-                        g_isSelectionActive = true;
-                        long exStyle = GetWindowLong(hWnd, GWL_EXSTYLE);
-                        exStyle &= ~WS_EX_TRANSPARENT;
-                        SetWindowLong(hWnd, GWL_EXSTYLE, exStyle);
-                        SetForegroundWindow(hWnd);
-                    } else {
-                        g_currentSelection = NONE;
-                        g_isSelectionActive = false;
-                        SetWindowLong(hWnd, GWL_EXSTYLE, GetWindowLong(hWnd, GWL_EXSTYLE) | WS_EX_TRANSPARENT);
-                    }
-                    break;
-                case 3:
-                    g_showCrosshair = !g_showCrosshair;
-                    break;
-        case 4:
-            g_currentAngle = 0.0f;
-            break;
-        case 5:
-            g_debugMode = !g_debugMode;
-            break;
+  case WM_HOTKEY: {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    switch (wParam) {
+    case 1: // Toggle Panel
+      if (IsIconic(g_hPanel))
+        ShowWindow(g_hPanel, SW_RESTORE);
+      else if (IsWindowVisible(g_hPanel))
+        ShowWindow(g_hPanel, SW_MINIMIZE);
+      else
+        ShowWindow(g_hPanel, SW_SHOW);
+      break;
+    case 2: // ROI Select Toggle
+      if (g_currentSelection == NONE) {
+        CaptureDesktop(); // Capture before dimming
+        g_currentSelection = SELECTING_ROI;
+        g_isSelectionActive = true;
+        long exStyle = GetWindowLong(hWnd, GWL_EXSTYLE);
+        exStyle &= ~WS_EX_TRANSPARENT;
+        SetWindowLong(hWnd, GWL_EXSTYLE, exStyle);
+        SetForegroundWindow(hWnd);
+      } else {
+        g_currentSelection = NONE;
+        g_isSelectionActive = false;
+        SetWindowLong(hWnd, GWL_EXSTYLE,
+                      GetWindowLong(hWnd, GWL_EXSTYLE) | WS_EX_TRANSPARENT);
+      }
+      break;
+    case 3:
+      g_showCrosshair = !g_showCrosshair;
+      break;
+    case 4:
+      g_currentAngle = 0.0f;
+      break;
+    case 5:
+      g_debugMode = !g_debugMode;
+      break;
     }
+  }
     return 0;
-        case WM_LBUTTONDOWN:
-            if (g_currentSelection == SELECTING_ROI) {
-                POINT cur; GetCursorPos(&cur);
-                g_startPoint = cur;
-                g_selectionRect = { cur.x, cur.y, cur.x, cur.y };
-            } else if (g_currentSelection == SELECTING_COLOR) {
-                // STAGE 2: PRECISION COLOR PICK (Snap-Shot Bypass)
-                if (g_screenSnapshot) {
-                    HDC hdcScreen = GetDC(NULL);
-                    HDC hdcMem = CreateCompatibleDC(hdcScreen);
-                    HGDIOBJ hOld = SelectObject(hdcMem, g_screenSnapshot);
-                    
-                    POINT cur; GetCursorPos(&cur);
-                    COLORREF pixel = GetPixel(hdcMem, cur.x, cur.y);
-                    // Sync: GDI returns 0x00BBGGRR
-                    g_targetColor = pixel; 
-                    g_pickedColor = pixel;
+  case WM_LBUTTONDOWN: {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    if (g_currentSelection == SELECTING_ROI) {
+      POINT cur;
+      GetCursorPos(&cur);
+      g_startPoint = cur;
+      g_selectionRect = {cur.x, cur.y, cur.x, cur.y};
+    } else if (g_currentSelection == SELECTING_COLOR) {
+      // STAGE 2: PRECISION COLOR PICK (Snap-Shot Bypass)
+      if (g_screenSnapshot) {
+        HDC hdcScreen = GetDC(NULL);
+        if (!hdcScreen)
+          return 0;
+
+        HDC hdcMem = CreateCompatibleDC(hdcScreen);
+        if (!hdcMem) {
+          ReleaseDC(NULL, hdcScreen);
+          return 0;
+        }
+
+        HGDIOBJ hOld = SelectObject(hdcMem, g_screenSnapshot);
+        if (!hOld) {
+          DeleteDC(hdcMem);
+          ReleaseDC(NULL, hdcScreen);
+          return 0;
+        }
+
+        POINT cur;
+        GetCursorPos(&cur);
+        COLORREF pixel = GetPixel(hdcMem, cur.x, cur.y);
+        // Sync: GDI returns 0x00BBGGRR
+        g_targetColor = pixel;
+        g_pickedColor = pixel;
 
-                    SelectObject(hdcMem, hOld);
-                    DeleteDC(hdcMem);
-                    ReleaseDC(NULL, hdcScreen);
-                }
+        SelectObject(hdcMem, hOld);
+        DeleteDC(hdcMem);
+        ReleaseDC(NULL, hdcScreen);
+      }
 
-                // Finalize and Exit Selection
-                g_currentSelection = NONE;
-                g_isSelectionActive = false;
-                SetWindowLong(hWnd, GWL_EXSTYLE, GetWindowLong(hWnd, GWL_EXSTYLE) | WS_EX_TRANSPARENT);
+      // Finalize and Exit Selection
+      g_currentSelection = NONE;
+      g_isSelectionActive = false;
+      SetWindowLong(hWnd, GWL_EXSTYLE,
+                    GetWindowLong(hWnd, GWL_EXSTYLE) | WS_EX_TRANSPARENT);
 
-                if (!g_allProfiles.empty()) {
-                    Profile& p = g_allProfiles[g_selectedProfileIdx];
-                    p.target_color = g_targetColor;
-                    p.roi_x = min(g_selectionRect.left, g_selectionRect.right);
-                    p.roi_y = min(g_selectionRect.top, g_selectionRect.bottom);
-                    p.roi_w = abs(g_selectionRect.right - g_selectionRect.left);
-                    p.roi_h = abs(g_selectionRect.bottom - g_selectionRect.top);
-                    
-                    // Save to the actual profile path
-                    std::wstring profilePath = L"profiles/" + p.name + L".json";
-                    p.Save(profilePath);
-                    
-                    // Also maintain the legacy 'last_calibrated' for quick-load logic if needed
-                    p.Save(L"profiles/last_calibrated.json");
-                }
-            }
-            return 0;
+      if (!g_allProfiles.empty()) {
+        Profile &p = g_allProfiles[g_selectedProfileIdx];
+        p.target_color = g_targetColor;
+        p.roi_x = min(g_selectionRect.left, g_selectionRect.right);
+        p.roi_y = min(g_selectionRect.top, g_selectionRect.bottom);
+        p.roi_w = abs(g_selectionRect.right - g_selectionRect.left);
+        p.roi_h = abs(g_selectionRect.bottom - g_selectionRect.top);
+
+        // Save to the actual profile path
+        std::wstring profilePath = L"profiles/" + p.name + L".json";
+        p.Save(profilePath);
+
+        // Also maintain the legacy 'last_calibrated' for quick-load logic if
+        // needed
+        p.Save(L"profiles/last_calibrated.json");
+      }
+    }
+  }
+    return 0;
 
-        case WM_MOUSEMOVE:
-            if (g_currentSelection != NONE) {
-                if (g_currentSelection == SELECTING_ROI && (wParam & MK_LBUTTON)) {
-                    POINT cur; GetCursorPos(&cur);
-                    g_selectionRect.right = cur.x;
-                    g_selectionRect.bottom = cur.y;
-                }
-                InvalidateRect(hWnd, NULL, FALSE);
-            }
-            return 0;
+  case WM_MOUSEMOVE: {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    if (g_currentSelection != NONE) {
+      if (g_currentSelection == SELECTING_ROI && (wParam & MK_LBUTTON)) {
+        POINT cur;
+        GetCursorPos(&cur);
+        g_selectionRect.right = cur.x;
+        g_selectionRect.bottom = cur.y;
+      }
+      InvalidateRect(hWnd, NULL, FALSE);
+    }
+  }
+    return 0;
 
-        case WM_LBUTTONUP:
-            if (g_currentSelection == SELECTING_ROI) {
-                g_currentSelection = SELECTING_COLOR;
-                InvalidateRect(hWnd, NULL, FALSE);
-            }
-            return 0;
+  case WM_LBUTTONUP: {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    if (g_currentSelection == SELECTING_ROI) {
+      g_currentSelection = SELECTING_COLOR;
+      InvalidateRect(hWnd, NULL, FALSE);
+    }
+  }
+    return 0;
 
-		case WM_INPUT: {
-            // THE FIX: Send the mouse delta to the logic engine
-            int dx = GetRawInputDeltaX(lParam);
-            g_logic.Update(dx);
-            return 0;
-        }
+  case WM_INPUT: {
+    // THE FIX: Send the mouse delta to the logic engine
+    int dx = GetRawInputDeltaX(lParam);
+    g_logic.Update(dx);
+    return 0;
+  }
 
-        case WM_TIMER: {
-            CURSORINFO ci = { sizeof(CURSORINFO) };
-            if (GetCursorInfo(&ci)) {
-                g_isCursorVisible = (ci.flags & CURSOR_SHOWING);
-            }
-            InvalidateRect(hWnd, NULL, FALSE);
-            return 0;
-        }
+  case WM_TIMER: {
+    CURSORINFO ci = {sizeof(CURSORINFO)};
+    if (GetCursorInfo(&ci)) {
+      std::lock_guard<std::mutex> lock(g_stateMutex);
+      g_isCursorVisible = (ci.flags & CURSOR_SHOWING);
+    }
+    InvalidateRect(hWnd, NULL, FALSE);
+    return 0;
+  }
 
-        case WM_PAINT:
-            DrawOverlay(hWnd, g_logic.GetAngle(), g_status.c_str(), g_detectionRatio, g_showCrosshair);
-            return 0;
+  case WM_PAINT: {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    DrawOverlay(hWnd, g_logic.GetAngle(), g_status.c_str(), g_detectionRatio,
+                g_showCrosshair);
+  }
+    return 0;
 
-        case WM_DESTROY:
-            g_running = false;
-            PostQuitMessage(0);
-            return 0;
+  case WM_DESTROY:
+    g_running = false;
+    PostQuitMessage(0);
+    return 0;
 
-        default:
-            return DefWindowProc(hWnd, message, wParam, lParam);
-    }
+  default:
+    return DefWindowProc(hWnd, message, wParam, lParam);
+  }
 }
 
-int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow) {
-    // Phase 0: Silent Update & Relaunch Sequence
+int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance,
+                   LPSTR lpCmdLine, int nCmdShow) {
+  // Phase 0: Silent Update & Relaunch Sequence
+  try {
     if (CheckForUpdates()) {
-        std::wstring verStr = std::wstring(g_latestVersionOnline.begin(), g_latestVersionOnline.end());
-        // User requested: Download the latest .exe from GitHub to update_tmp.exe
-        std::wstring downloadUrl = L"https://github.com/MahanYTT/BetterAngle/releases/latest/download/BetterAngle.exe";
-        if (DownloadUpdate(downloadUrl, L"update_tmp.exe")) {
-            ApplyUpdateAndRestart();
-        }
+      std::wstring verStr = std::wstring(g_latestVersionOnline.begin(),
+                                         g_latestVersionOnline.end());
+      // User requested: Download the latest .exe from GitHub to update_tmp.exe
+      std::wstring downloadUrl = L"https://github.com/MahanYTT/BetterAngle/"
+                                 L"releases/latest/download/BetterAngle.exe";
+      if (DownloadUpdate(downloadUrl, L"update_tmp.exe")) {
+        ApplyUpdateAndRestart();
+      }
     }
+  } catch (const std::exception &e) {
+    MessageBox(NULL, L"Failed to check for updates", L"Error", MB_OK);
+  }
 
-    GdiplusStartupInput gdiplusStartupInput;
-    GdiplusStartup(&g_gdiplusToken, &gdiplusStartupInput, NULL);
+  GdiplusStartupInput gdiplusStartupInput;
+  ULONG_PTR gdiplusToken;
+  GdiplusStartup(&gdiplusToken, &gdiplusStartupInput, NULL);
+  g_gdiplusToken = gdiplusToken;
 
-    // Phase 1: Startup Sequence (Splash)
+  // Phase 1: Startup Sequence (Splash)
+  try {
     ShowSplashLoader(hInstance);
+  } catch (const std::exception &e) {
+    MessageBox(NULL, L"Failed to show splash loader", L"Error", MB_OK);
+  }
 
-    // Initial Load (Syncing was handled or represented in Splash)
-    g_allProfiles = GetProfiles(L"profiles");
-    if (!g_allProfiles.empty()) {
+  // Initial Load (Syncing was handled or represented in Splash)
+  {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    try {
+      g_allProfiles = GetProfiles(L"profiles");
+      if (!g_allProfiles.empty()) {
         g_currentProfile = g_allProfiles[0];
         g_logic.SetScale(g_currentProfile.scale_normal);
+      }
+    } catch (const std::exception &e) {
+      MessageBox(NULL, L"Failed to load profiles", L"Error", MB_OK);
+      return 1;
     }
+  }
 
-    // Phase 2: Create Control Panel (Interactive)
+  // Phase 2: Create Control Panel (Interactive)
+  try {
     g_hPanel = CreateControlPanel(hInstance);
-    
-    // Phase 3: Create HUD Window (Transparent Overlay)
-    WNDCLASS wc = { 0 };
+    if (!g_hPanel) {
+      MessageBox(NULL, L"Failed to create Control Panel", L"Error", MB_OK);
+      return 1;
+    }
+  } catch (const std::exception &e) {
+    MessageBox(NULL, L"Failed to create Control Panel", L"Error", MB_OK);
+    return 1;
+  }
+
+  // Phase 3: Create HUD Window (Transparent Overlay)
+  try {
+    WNDCLASS wc = {0};
     wc.lpfnWndProc = HUDWndProc;
     wc.hInstance = hInstance;
     wc.hCursor = LoadCursor(NULL, IDC_ARROW);
     wc.lpszClassName = L"BetterAngleHUD";
-    RegisterClass(&wc);
+    if (!RegisterClass(&wc)) {
+      MessageBox(NULL, L"Failed to register HUD window class", L"Error", MB_OK);
+      return 1;
+    }
 
     int screenW = GetSystemMetrics(SM_CXSCREEN);
     int screenH = GetSystemMetrics(SM_CYSCREEN);
 
-    g_hHUD = CreateWindowEx(
-        WS_EX_TOPMOST | WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_TOOLWINDOW,
-        L"BetterAngleHUD", L"BetterAngle HUD",
-        WS_POPUP,
-        0, 0, screenW, screenH,
-        NULL, NULL, hInstance, NULL
-    );
+    g_hHUD = CreateWindowEx(WS_EX_TOPMOST | WS_EX_LAYERED | WS_EX_TRANSPARENT |
+                                WS_EX_TOOLWINDOW,
+                            L"BetterAngleHUD", L"BetterAngle HUD", WS_POPUP, 0,
+                            0, screenW, screenH, NULL, NULL, hInstance, NULL);
+    if (!g_hHUD) {
+      MessageBox(NULL, L"Failed to create HUD window", L"Error", MB_OK);
+      return 1;
+    }
+
     RegisterRawMouse(g_hHUD);
     ShowWindow(g_hHUD, SW_SHOW);
     UpdateWindow(g_hHUD);
     SetTimer(g_hHUD, 1, 25, NULL);
+  } catch (const std::exception &e) {
+    MessageBox(NULL, L"Failed to create HUD window", L"Error", MB_OK);
+    return 1;
+  }
 
-    std::thread detThread(DetectorThread);
+  std::thread detThread;
+  try {
+    detThread = std::thread(DetectorThread);
+  } catch (const std::exception &e) {
+    MessageBox(NULL, L"Failed to start detector thread", L"Error", MB_OK);
+    if (g_hHUD) {
+      DestroyWindow(g_hHUD);
+    }
+    if (g_hPanel) {
+      DestroyWindow(g_hPanel);
+    }
+    return 1;
+  }
 
-    MSG msg;
-    while (GetMessage(&msg, NULL, 0, 0)) {
-        TranslateMessage(&msg);
-        DispatchMessage(&msg);
-        
-        // If Panel is closed by user (WM_QUIT from Panel should not happen but WM_DESTROY does)
-        if (!IsWindow(g_hPanel) && !IsWindow(g_hHUD)) break;
+  MSG msg;
+  while (GetMessage(&msg, NULL, 0, 0)) {
+    try {
+      TranslateMessage(&msg);
+      DispatchMessage(&msg);
+
+      // If Panel is closed by user (WM_QUIT from Panel should not happen but
+      // WM_DESTROY does)
+      if (!IsWindow(g_hPanel) && !IsWindow(g_hHUD))
+        break;
+    } catch (const std::exception &e) {
+      MessageBox(NULL, L"Error in message loop", L"Error", MB_OK);
+      break;
     }
+  }
 
-    g_running = false;
-    if (detThread.joinable()) detThread.join();
+  g_running = false;
+  if (detThread.joinable())
+    detThread.join();
+
+  if (g_hHUD) {
+    DestroyWindow(g_hHUD);
+    g_hHUD = NULL;
+  }
+  if (g_hPanel) {
+    DestroyWindow(g_hPanel);
+    g_hPanel = NULL;
+  }
+
+  if (g_gdiplusToken) {
     GdiplusShutdown(g_gdiplusToken);
-    return (int)msg.wParam;
+    g_gdiplusToken = 0;
+  }
+
+  {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    if (g_screenSnapshot) {
+      DeleteObject(g_screenSnapshot);
+      g_screenSnapshot = NULL;
+    }
+    g_allProfiles.clear();
+    g_selectedProfileIdx = 0;
+    g_currentProfile = Profile();
+    g_currentSelection = NONE;
+    g_isSelectionActive = false;
+    g_isDiving = false;
+    g_showROIBox = true;
+    g_currentTab = 0;
+    g_detectionRatio = 0.0f;
+    g_isCheckingForUpdates = false;
+    g_updateSpinAngle = 0.0f;
+    g_updateAvailable = false;
+    g_showCrosshair = false;
+    g_pickedColor = RGB(255, 255, 255);
+    g_targetColor = RGB(255, 255, 255);
+    g_latestVersion = 4.920f;
+    g_latestName = L"Pending Scan";
+    g_selectionRect = {0, 0, 0, 0};
+    g_startPoint = {0};
+    g_status = "Connected (v" VERSION_STR " Pro)";
+    g_latestVersionOnline = "v" VERSION_STR;
+    g_currentAngle = 0.0f;
+    g_debugMode = false;
+    g_isCursorVisible = false;
+    g_logic.SetScale(0.0);
+    g_logic.SetZero();
+  }
+
+  return (int)msg.wParam;
 }
Index: include/shared/State.h
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#ifndef STATE_H\r\n#define STATE_H\r\n\r\n#include <windows.h>\r\n#include <atomic>\r\n#include <vector>\r\n#include <string>\r\n#include \"shared/Logic.h\"\r\n\r\n// --- Inside State.h ---\r\n#define STRING_HELPER(x) #x\r\n#define TO_STRING(x) STRING_HELPER(x)\r\n#define TO_WSTRING(x) L\"\" TO_STRING(x)\r\n\r\n// Define the \"raw\" version if not passed by compiler flags\r\n#ifndef APP_VERSION\r\n    #define APP_VERSION 4.9.36\r\n#endif\r\n\r\n// This creates the actual strings \"4.9.36\" and L\"4.9.36\"\r\n#define VERSION_STR TO_STRING(APP_VERSION)\r\n#define VERSION_WSTR TO_WSTRING(APP_VERSION)\r\n\r\n// HUD & Global Shared State\r\nenum SelectionState { NONE, SELECTING_ROI, SELECTING_COLOR };\r\nextern SelectionState g_currentSelection;\r\nextern bool g_isSelectionActive;\r\nextern HBITMAP g_screenSnapshot;\r\nextern bool g_isDiving;\r\nextern bool g_showROIBox;\r\nextern int g_currentTab;\r\nextern float g_detectionRatio;\r\nextern bool g_isCheckingForUpdates;\r\nextern float g_updateSpinAngle;\r\nextern bool g_updateAvailable;\r\nextern bool g_showCrosshair;\r\nextern COLORREF g_pickedColor;\r\nextern COLORREF g_targetColor;\r\nextern float g_latestVersion;\r\nextern std::wstring g_latestName;\r\nextern RECT g_selectionRect;\r\nextern POINT g_startPoint;\r\nextern std::string g_status;\r\nextern std::string g_latestVersionOnline;\r\nextern float g_currentAngle;\r\nextern bool g_debugMode;\r\nextern bool g_isCursorVisible;\r\nextern AngleLogic g_logic;\r\n\r\n#endif // STATE_H\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/include/shared/State.h b/include/shared/State.h
--- a/include/shared/State.h	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/include/shared/State.h	(date 1775748098232)
@@ -1,28 +1,26 @@
 #ifndef STATE_H
 #define STATE_H
 
-#include <windows.h>
-#include <atomic>
-#include <vector>
+#include <mutex>
 #include <string>
+#include <windows.h>
+
 #include "shared/Logic.h"
 
-// --- Inside State.h ---
 #define STRING_HELPER(x) #x
 #define TO_STRING(x) STRING_HELPER(x)
 #define TO_WSTRING(x) L"" TO_STRING(x)
 
-// Define the "raw" version if not passed by compiler flags
 #ifndef APP_VERSION
-    #define APP_VERSION 4.9.36
+#define APP_VERSION 4.9.36
 #endif
 
-// This creates the actual strings "4.9.36" and L"4.9.36"
 #define VERSION_STR TO_STRING(APP_VERSION)
 #define VERSION_WSTR TO_WSTRING(APP_VERSION)
 
-// HUD & Global Shared State
 enum SelectionState { NONE, SELECTING_ROI, SELECTING_COLOR };
+
+extern std::mutex g_stateMutex;
 extern SelectionState g_currentSelection;
 extern bool g_isSelectionActive;
 extern HBITMAP g_screenSnapshot;
@@ -47,4 +45,4 @@
 extern bool g_isCursorVisible;
 extern AngleLogic g_logic;
 
-#endif // STATE_H
+#endif
Index: src/shared/State.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"shared/State.h\"\r\n#include \"shared/Logic.h\"\r\n\r\nSelectionState g_currentSelection = NONE;\r\nbool g_isSelectionActive = false;\r\nHBITMAP g_screenSnapshot = NULL;\r\nbool g_isDiving = false;\r\nbool g_showROIBox = true;\r\nint g_currentTab = 0;\r\nfloat g_detectionRatio = 0.0f;\r\nbool g_isCheckingForUpdates = false;\r\nfloat g_updateSpinAngle = 0.0f;\r\nbool g_updateAvailable = false;\r\nbool g_showCrosshair = false;\r\nCOLORREF g_pickedColor = RGB(255, 255, 255);\r\nCOLORREF g_targetColor = RGB(255, 255, 255);\r\nfloat g_latestVersion = 4.920f; // v4.9.20 format fallback\r\nstd::wstring g_latestName = L\"Pending Scan\";\r\nRECT g_selectionRect = { 0, 0, 0, 0 };\r\nPOINT g_startPoint = { 0 };\r\n\r\n\r\nstd::string g_status = \"Connected (v\" VERSION_STR \" Pro)\";\r\nstd::string g_latestVersionOnline = \"v\" VERSION_STR;\r\nfloat g_currentAngle = 0.0f;\r\nbool g_debugMode = false;\r\nbool g_isCursorVisible = false;\r\nAngleLogic g_logic(800, 6.5);\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/shared/State.cpp b/src/shared/State.cpp
--- a/src/shared/State.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/shared/State.cpp	(date 1775748098236)
@@ -1,6 +1,6 @@
 #include "shared/State.h"
-#include "shared/Logic.h"
 
+std::mutex g_stateMutex;
 SelectionState g_currentSelection = NONE;
 bool g_isSelectionActive = false;
 HBITMAP g_screenSnapshot = NULL;
@@ -14,12 +14,10 @@
 bool g_showCrosshair = false;
 COLORREF g_pickedColor = RGB(255, 255, 255);
 COLORREF g_targetColor = RGB(255, 255, 255);
-float g_latestVersion = 4.920f; // v4.9.20 format fallback
+float g_latestVersion = 4.920f;
 std::wstring g_latestName = L"Pending Scan";
-RECT g_selectionRect = { 0, 0, 0, 0 };
-POINT g_startPoint = { 0 };
-
-
+RECT g_selectionRect = {0, 0, 0, 0};
+POINT g_startPoint = {0};
 std::string g_status = "Connected (v" VERSION_STR " Pro)";
 std::string g_latestVersionOnline = "v" VERSION_STR;
 float g_currentAngle = 0.0f;
Index: src/shared/Updater.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"shared/Updater.h\"\r\n#include \"shared/State.h\"\r\n#include <windows.h>\r\n#include <wininet.h>\r\n#include <urlmon.h>\r\n#include <fstream>\r\n#include <string>\r\n\r\n#pragma comment(lib, \"wininet.lib\")\r\n#pragma comment(lib, \"urlmon.lib\")\r\n\r\n#ifndef VERSION_STR\r\n#define VERSION_STR TO_STRING(APP_VERSION)\r\n#define VERSION_WSTR TO_WSTRING(APP_VERSION)\r\n#endif\r\n#ifndef APP_VERSION_STR\r\n#define APP_VERSION_STR \"4.9.36\"\r\n#endif\r\n\r\nbool CheckForUpdates() {\r\n    g_isCheckingForUpdates = true;\r\n    std::string userAgent = \"BetterAngle/\" APP_VERSION_STR;\r\n    HINTERNET hInternet = InternetOpenA(userAgent.c_str(), INTERNET_OPEN_TYPE_DIRECT, NULL, NULL, 0);\r\n    if (!hInternet) return false;\r\n\r\n    HINTERNET hUrl = InternetOpenUrlA(hInternet, \"https://raw.githubusercontent.com/MahanYTT/BetterAngle/main/VERSION\", NULL, 0, INTERNET_FLAG_RELOAD, 0);\r\n    if (!hUrl) {\r\n        InternetCloseHandle(hInternet);\r\n        return false;\r\n    }\r\n\r\n    char buffer[128];\r\n    DWORD bytesRead = 0;\r\n    std::string newVersion;\r\n    if (InternetReadFile(hUrl, buffer, sizeof(buffer) - 1, &bytesRead) && bytesRead > 0) {\r\n        buffer[bytesRead] = '\\0';\r\n        newVersion = buffer;\r\n        // Trim any trailing whitespace/newlines\r\n        size_t last = newVersion.find_last_not_of(\" \\n\\r\\t\");\r\n        if (last != std::string::npos) newVersion = newVersion.substr(0, last + 1);\r\n    }\r\n\r\n    InternetCloseHandle(hUrl);\r\n    InternetCloseHandle(hInternet);\r\n\r\n    if (!newVersion.empty()) {\r\n        g_latestVersionOnline = \"v\" + newVersion;\r\n        \r\n        try {\r\n            g_latestVersion = std::stof(newVersion);\r\n        } catch (...) {\r\n            g_latestVersion = 4.920f;\r\n        }\r\n        \r\n        g_latestName = L\"GitHub Main Branch (v\" + std::wstring(newVersion.begin(), newVersion.end()) + L\")\";\r\n        \r\n        if (g_latestVersionOnline != (\"v\" APP_VERSION_STR)) {\r\n            g_updateAvailable = true;\r\n        } else {\r\n            g_updateAvailable = false;\r\n        }\r\n        \r\n        g_isCheckingForUpdates = false;\r\n        return g_updateAvailable;\r\n    }\r\n\r\n    g_isCheckingForUpdates = false;\r\n    return false;\r\n}\r\n\r\nbool DownloadUpdate(const std::wstring& url, const std::wstring& dest) {\r\n    if (URLDownloadToFileW(NULL, url.c_str(), dest.c_str(), 0, NULL) == S_OK) {\r\n        return true;\r\n    }\r\n    return false;\r\n}\r\n\r\nvoid ApplyUpdateAndRestart() {\r\n    // FIX 1: Use standard ofstream (ASCII) so cmd.exe parses it safely\r\n    std::ofstream bat(\"cleanup.bat\");\r\n\r\n    bat << \"@echo off\\n\";\r\n\r\n    // FIX 2: Wait 2 seconds for BetterAngle.exe to fully close and release file locks\r\n    bat << \"timeout /t 2 /nobreak >nul\\n\";\r\n\r\n    // Force kill just in case, but DO NOT loop infinitely\r\n    bat << \"taskkill /F /IM BetterAngle.exe >nul 2>&1\\n\";\r\n\r\n    // Swap the files\r\n    bat << \"del BetterAngle.exe\\n\";\r\n    bat << \"rename update_tmp.exe BetterAngle.exe\\n\";\r\n\r\n    // FIX 3: Start securely with empty title quotes to prevent path issues\r\n    bat << \"start \\\"\\\" \\\"BetterAngle.exe\\\"\\n\";\r\n\r\n    // Self-destruct the batch file\r\n    bat << \"del \\\"%~f0\\\"\\n\";\r\n\r\n    bat.close();\r\n\r\n    // Execute the new ASCII batch script silently\r\n    ShellExecuteA(NULL, \"open\", \"cleanup.bat\", NULL, NULL, SW_HIDE);\r\n\r\n    // Exit the current instance immediately so the batch file can delete it\r\n    exit(0);\r\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/shared/Updater.cpp b/src/shared/Updater.cpp
--- a/src/shared/Updater.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/shared/Updater.cpp	(date 1775748109575)
@@ -80,28 +80,19 @@
     std::ofstream bat("cleanup.bat");
 
     bat << "@echo off\n";
-
     // FIX 2: Wait 2 seconds for BetterAngle.exe to fully close and release file locks
     bat << "timeout /t 2 /nobreak >nul\n";
-
-    // Force kill just in case, but DO NOT loop infinitely
+    // Force kill just in case, but DO NOT loop on it
     bat << "taskkill /F /IM BetterAngle.exe >nul 2>&1\n";
-
-    // Swap the files
     bat << "del BetterAngle.exe\n";
     bat << "rename update_tmp.exe BetterAngle.exe\n";
-
-    // FIX 3: Start securely with empty title quotes to prevent path issues
+    // FIX 3: Start securely with empty title quotes to prevent path spacing issues
     bat << "start \"\" \"BetterAngle.exe\"\n";
-
-    // Self-destruct the batch file
     bat << "del \"%~f0\"\n";
 
     bat.close();
 
-    // Execute the new ASCII batch script silently
+    // Execute the new ASCII batch script
     ShellExecuteA(NULL, "open", "cleanup.bat", NULL, NULL, SW_HIDE);
-
-    // Exit the current instance immediately so the batch file can delete it
     exit(0);
 }
\ No newline at end of file
Index: src/shared/ControlPanel.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"shared/State.h\"\r\n#include \"shared/Overlay.h\"\r\n#include \"shared/Config.h\"\r\n#include \"shared/ControlPanel.h\"\r\n#include \"shared/Updater.h\"\r\n#include <thread>\r\n#include <d2d1.h>\r\n#include <dwrite.h>\r\n#include <string>\r\n\r\n#include <commdlg.h>\r\n#pragma comment(lib, \"comdlg32.lib\")\r\n#pragma comment(lib, \"d2d1.lib\")\r\n#pragma comment(lib, \"dwrite.lib\")\r\n\r\n#ifndef APP_VERSION_STR\r\n#define APP_VERSION_STR \"4.9.36\"\r\n#endif\r\n#ifndef APP_VERSION_WSTR\r\n#define APP_VERSION_WSTR L\"4.9.36\"\r\n#endif\r\n\r\n#ifndef VERSION_STR\r\n#define VERSION_STR TO_STRING(APP_VERSION)\r\n#define VERSION_WSTR TO_WSTRING(APP_VERSION)\r\n#endif\r\nusing namespace D2D1;\r\n\r\nID2D1Factory* g_pD2DFactory = NULL;\r\nIDWriteFactory* g_pDWriteFactory = NULL;\r\nID2D1HwndRenderTarget* g_pRenderTarget = NULL;\r\n\r\n// UI State\r\n// extern declared in State.h\r\n\r\nvoid InitD2D(HWND hWnd) {\r\n    if (!g_pD2DFactory) {\r\n        D2D1CreateFactory(D2D1_FACTORY_TYPE_SINGLE_THREADED, &g_pD2DFactory);\r\n        DWriteCreateFactory(DWRITE_FACTORY_TYPE_SHARED, __uuidof(IDWriteFactory), (IUnknown**)&g_pDWriteFactory);\r\n    }\r\n    RECT rc; GetClientRect(hWnd, &rc);\r\n    D2D1_SIZE_U size = D2D1::SizeU(rc.right - rc.left, rc.bottom - rc.top);\r\n    if (g_pRenderTarget) g_pRenderTarget->Release();\r\n    g_pD2DFactory->CreateHwndRenderTarget(D2D1::RenderTargetProperties(), D2D1::HwndRenderTargetProperties(hWnd, size), &g_pRenderTarget);\r\n}\r\n\r\nvoid DrawD2DButton(ID2D1HwndRenderTarget* rt, D2D1_RECT_F rect, const wchar_t* text, D2D1_COLOR_F color) {\r\n    ID2D1SolidColorBrush* pBrush = NULL;\r\n    rt->CreateSolidColorBrush(color, &pBrush);\r\n    \r\n    rt->FillRoundedRectangle(D2D1::RoundedRect(rect, 8.0f, 8.0f), pBrush);\r\n    \r\n    ID2D1SolidColorBrush* pWhite = NULL;\r\n    rt->CreateSolidColorBrush(ColorF(ColorF::White), &pWhite);\r\n    \r\n    IDWriteTextFormat* pTextFormat = NULL;\r\n    g_pDWriteFactory->CreateTextFormat(L\"Segoe UI Variable Display\", NULL, DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 14.0f, L\"en-us\", &pTextFormat);\r\n    pTextFormat->SetTextAlignment(DWRITE_TEXT_ALIGNMENT_CENTER);\r\n    pTextFormat->SetParagraphAlignment(DWRITE_PARAGRAPH_ALIGNMENT_CENTER);\r\n    \r\n    rt->DrawText(text, (UINT32)wcslen(text), pTextFormat, rect, pWhite);\r\n    \r\n    pTextFormat->Release();\r\n    pWhite->Release();\r\n    pBrush->Release();\r\n}\r\n\r\nHWND CreateControlPanel(HINSTANCE hInst) {\r\n    WNDCLASS wc = { 0 };\r\n    wc.lpfnWndProc = ControlPanelWndProc;\r\n    wc.hInstance = hInst;\r\n    wc.hCursor = LoadCursor(NULL, IDC_ARROW);\r\n    wc.hbrBackground = (HBRUSH)(COLOR_WINDOW + 1);\r\n    wc.lpszClassName = L\"BetterAngleControlPanel\";\r\n    RegisterClass(&wc);\r\n\r\n    int w = 420, h = 680;\r\n    HWND hPanel = CreateWindowEx(\r\n        WS_EX_TOPMOST | WS_EX_APPWINDOW,\r\n        L\"BetterAngleControlPanel\", L\"BetterAngle Pro | Global Command Center\",\r\n        WS_OVERLAPPED | WS_CAPTION | WS_SYSMENU | WS_MINIMIZEBOX,\r\n        CW_USEDEFAULT, CW_USEDEFAULT, w, h,\r\n        NULL, NULL, hInst, NULL\r\n    );\r\n\r\n    ShowWindow(hPanel, SW_SHOW);\r\n    UpdateWindow(hPanel);\r\n    return hPanel;\r\n}\r\n\r\nLRESULT CALLBACK ControlPanelWndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam) {\r\n    switch (message) {\r\n        case WM_CREATE:\r\n            InitD2D(hWnd);\r\n            SetTimer(hWnd, 1, 16, NULL); // 60FPS Refresh Rate\r\n            return 0;\r\n        case WM_SIZE:\r\n            InitD2D(hWnd);\r\n            return 0;\r\n        case WM_LBUTTONDOWN: {\r\n            int x = LOWORD(lParam), y = HIWORD(lParam);\r\n            if (y >= 90 && y <= 120) {\r\n                if (x >= 40 && x <= 200) g_currentTab = 0;\r\n                else if (x >= 210 && x <= 380) g_currentTab = 1;\r\n            }\r\n            if (g_currentTab == 1) {\r\n                if (x >= 40 && x <= 380 && y >= 320 && y <= 370) {\r\n                    if (g_updateAvailable) {\r\n                        // FIX: Immediately lock the state to prevent multiple threads\r\n                        g_updateAvailable = false;\r\n\r\n                        std::thread([]() {\r\n                            std::wstring verStr = std::wstring(g_latestVersionOnline.begin(), g_latestVersionOnline.end());\r\n                            std::wstring downloadUrl = L\"https://github.com/MahanYTT/BetterAngle/releases/latest/download/BetterAngle.exe\";\r\n                            if (DownloadUpdate(downloadUrl, L\"update_tmp.exe\")) {\r\n                                ApplyUpdateAndRestart();\r\n                            }\r\n                        }).detach();\r\n                    } else {\r\n                        g_isCheckingForUpdates = true;\r\n                        std::thread(CheckForUpdates).detach();\r\n                    }\r\n                }\r\n                if (g_updateAvailable && x >= 40 && x <= 380 && y >= 380 && y <= 430) {\r\n                    ShellExecuteW(0, L\"open\", L\"https://github.com/MahanYTT/BetterAngle/releases/latest\", 0, 0, SW_SHOW);\r\n                }\r\n                if (g_latestVersion > 4.92f && x >= 40 && x <= 380 && y >= 270 && y <= 290) {\r\n                    ShellExecuteW(0, L\"open\", L\"https://github.com/MahanYTT/BetterAngle/releases\", 0, 0, SW_SHOW);\r\n                }\r\n            }\r\n\r\n            if (x >= 40 && x <= 380 && y >= 580 && y <= 630) {\r\n                PostQuitMessage(0);\r\n            }\r\n            return 0;\r\n        } // <-- Make sure this closing brace exists!\r\n        case WM_PAINT: {\r\n            if (!g_pRenderTarget) return 0;\r\n            g_pRenderTarget->BeginDraw();\r\n            g_pRenderTarget->Clear(D2D1::ColorF(0.05f, 0.06f, 0.07f));\r\n\r\n            ID2D1SolidColorBrush* pWhite = NULL;\r\n            g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(D2D1::ColorF::White), &pWhite);\r\n            ID2D1SolidColorBrush* pGrey = NULL;\r\n            g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(0.6f, 0.6f, 0.6f), &pGrey);\r\n            ID2D1SolidColorBrush* pBlue = NULL;\r\n            g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(0.3f, 0.7f, 1.0f), &pBlue);\r\n\r\n            IDWriteTextFormat* pTitleFormat = NULL;\r\n            g_pDWriteFactory->CreateTextFormat(L\"Segoe UI Variable Display\", NULL, DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 24.0f, L\"en-us\", &pTitleFormat);\r\n            IDWriteTextFormat* pHeaderFormat = NULL;\r\n            g_pDWriteFactory->CreateTextFormat(L\"Segoe UI Variable Display\", NULL, DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 16.0f, L\"en-us\", &pHeaderFormat);\r\n            IDWriteTextFormat* pVerFormat = NULL;\r\n            g_pDWriteFactory->CreateTextFormat(L\"Segoe UI Variable Display\", NULL, DWRITE_FONT_WEIGHT_REGULAR, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 13.0f, L\"en-us\", &pVerFormat);\r\n\r\n            g_pRenderTarget->DrawText(L\"Pro Command Center\", 18, pTitleFormat, D2D1::RectF(40, 40, 380, 80), pWhite);\r\n            \r\n            DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 90, 200, 120), L\"GENERAL / BINDS\", g_currentTab == 0 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));\r\n            DrawD2DButton(g_pRenderTarget, D2D1::RectF(210, 90, 380, 120), L\"UPDATES\", g_currentTab == 1 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));\r\n\r\n            if (g_currentTab == 0) {\r\n                g_pRenderTarget->DrawText(L\"CURRENT KEYBINDS\", 16, pHeaderFormat, D2D1::RectF(40, 140, 380, 170), pWhite);\r\n                \r\n                // Target Color Preview Circle (Linked to g_targetColor)\r\n                ID2D1SolidColorBrush* pTargetBrush = NULL;\r\n                g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(GetRValue(g_targetColor)/255.0f, GetGValue(g_targetColor)/255.0f, GetBValue(g_targetColor)/255.0f), &pTargetBrush);\r\n                g_pRenderTarget->FillEllipse(D2D1::Ellipse(D2D1::Point2F(360.0f, 155.0f), 12.0f, 12.0f), pTargetBrush);\r\n                g_pRenderTarget->DrawEllipse(D2D1::Ellipse(D2D1::Point2F(360.0f, 155.0f), 12.0f, 12.0f), pGrey, 1.0f);\r\n                if (pTargetBrush) pTargetBrush->Release();\r\n\r\n                g_pRenderTarget->DrawText(L\"Precision Crosshair: F10\\nVisual ROI Selector: Ctrl + R\\nToggle ROI Box: F9\", 66, pVerFormat, D2D1::RectF(40, 170, 380, 240), pGrey);\r\n                // Quick Guide for Workspace selection (v4.9.20: Removed hidden color picker)\r\n                g_pRenderTarget->DrawText(L\"Press CTRL+R to begin full-screen selection.\", 45, pVerFormat, D2D1::RectF(40, 250, 380, 290), pWhite);\r\n\r\n            } else if (g_currentTab == 1) {\r\n                g_pRenderTarget->DrawText(L\"SOFTWARE DASHBOARD\", 18, pHeaderFormat, D2D1::RectF(40, 140, 380, 170), pWhite);\r\n                \r\n                if (g_isCheckingForUpdates) {\r\n                    D2D1_POINT_2F center = D2D1::Point2F(365.0f, 155.0f);\r\n                    g_pRenderTarget->SetTransform(D2D1::Matrix3x2F::Rotation(g_updateSpinAngle, center));\r\n                    g_pRenderTarget->DrawEllipse(D2D1::Ellipse(center, 6.0f, 6.0f), pBlue, 2.0f);\r\n                    g_pRenderTarget->SetTransform(D2D1::Matrix3x2F::Identity());\r\n                }\r\n\r\n                std::wstring curVer = L\"Current Version: v\" APP_VERSION_WSTR L\" (Live Pro Build)\";\r\n                \r\n                // Use the real version string fetched from GitHub\r\n                std::wstring latestVerStr = std::wstring(g_latestVersionOnline.begin(), g_latestVersionOnline.end());\r\n                std::wstring latestVer = L\"Latest Found: \" + latestVerStr + L\" (\" + g_latestName + L\")\";\r\n\r\n                g_pRenderTarget->DrawText(curVer.c_str(), (UINT32)curVer.length(), pVerFormat, D2D1::RectF(40, 170, 380, 190), pGrey);\r\n                g_pRenderTarget->DrawText(latestVer.c_str(), (UINT32)latestVer.length(), pVerFormat, D2D1::RectF(40, 195, 380, 215), pGrey);\r\n\r\n                // Ensure it requires a version higher than 4.9.2 to trigger future updates\r\n                if (g_updateAvailable) {\r\n                    std::wstring changelog = L\"BetterAngle v\" APP_VERSION_WSTR L\" is now available online!\\nNewest Stable Version.\";\r\n                    g_pRenderTarget->DrawText(changelog.c_str(), (UINT32)changelog.length(), pVerFormat, D2D1::RectF(40, 230, 380, 260), pWhite);\r\n                    std::wstring viewFull = L\"View Full Changelog ->\";\r\n                    g_pRenderTarget->DrawText(viewFull.c_str(), (UINT32)viewFull.length(), pVerFormat, D2D1::RectF(40, 270, 380, 290), pBlue);\r\n                    DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 380, 380, 430), L\"DOWNLOAD UPDATE\", D2D1::ColorF(0.0f, 0.5f, 0.8f));\r\n                }\r\n                DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 320, 380, 370), g_updateAvailable ? L\"INSTALL UPDATE NOW\" : L\"DOWNLOAD AND INSTALL NOW\", D2D1::ColorF(0.15f, 0.17f, 0.2f));\r\n            }\r\n            DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 90, 145, 120), L\"GENERAL\", g_currentTab == 0 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));\r\n            DrawD2DButton(g_pRenderTarget, D2D1::RectF(155, 90, 265, 120), L\"UPDATES\", g_currentTab == 1 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));\r\n            DrawD2DButton(g_pRenderTarget, D2D1::RectF(275, 90, 380, 120), L\"COLORS\", g_currentTab == 2 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));\r\n            if (g_currentTab == 2) {\r\n                g_pRenderTarget->DrawText(L\"COLOR CONFIGURATION\", 19, pHeaderFormat, D2D1::RectF(40, 140, 380, 170), pWhite);\r\n\r\n                // Preview of Active Color\r\n                g_pRenderTarget->DrawText(L\"Active Target Color:\", 20, pVerFormat, D2D1::RectF(40, 180, 250, 200), pGrey);\r\n\r\n                ID2D1SolidColorBrush* pPreviewBrush = NULL;\r\n                g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(GetRValue(g_targetColor)/255.0f, GetGValue(g_targetColor)/255.0f, GetBValue(g_targetColor)/255.0f), &pPreviewBrush);\r\n                g_pRenderTarget->FillRoundedRectangle(D2D1::RoundedRect(D2D1::RectF(260, 175, 380, 205), 4.0f, 4.0f), pPreviewBrush);\r\n                if (pPreviewBrush) pPreviewBrush->Release();\r\n\r\n                // Instructions\r\n                std::wstring colorDesc = L\"The algorithm focuses on this specific color value to calculate angles. Accuracy depends on your monitor's color profile.\";\r\n                g_pRenderTarget->DrawText(colorDesc.c_str(), (UINT32)colorDesc.length(), pVerFormat, D2D1::RectF(40, 220, 380, 280), pGrey);\r\n\r\n                // Interaction Button\r\n                DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 320, 380, 370), L\"OPEN COLOR PALETTE\", D2D1::ColorF(0.2f, 0.4f, 0.6f));\r\n            }\r\n\r\n            DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 580, 380, 630), L\"QUIT SUITE\", D2D1::ColorF(0.7f, 0.1f, 0.15f));\r\n\r\n            pVerFormat->Release();\r\n            pHeaderFormat->Release();\r\n            pTitleFormat->Release();\r\n            pBlue->Release();\r\n            pGrey->Release();\r\n            pWhite->Release();\r\n            g_pRenderTarget->EndDraw();\r\n            ValidateRect(hWnd, NULL);\r\n            return 0;\r\n        }\r\n        case WM_TIMER:\r\n            if (g_isCheckingForUpdates) {\r\n                g_updateSpinAngle += 10.0f;\r\n                if (g_updateSpinAngle >= 360.0f) g_updateSpinAngle = 0.0f;\r\n            }\r\n            InvalidateRect(hWnd, NULL, FALSE);\r\n            return 0;\r\n        case WM_CLOSE:\r\n            ShowWindow(hWnd, SW_MINIMIZE);\r\n            return 0;\r\n        case WM_DESTROY:\r\n            if (g_pRenderTarget) g_pRenderTarget->Release();\r\n            return 0;\r\n        default:\r\n            return DefWindowProc(hWnd, message, wParam, lParam);\r\n    }\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/shared/ControlPanel.cpp b/src/shared/ControlPanel.cpp
--- a/src/shared/ControlPanel.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/shared/ControlPanel.cpp	(date 1775748098233)
@@ -1,12 +1,12 @@
+#include "shared/ControlPanel.h"
+#include "shared/Config.h"
+#include "shared/Overlay.h"
 #include "shared/State.h"
-#include "shared/Overlay.h"
-#include "shared/Config.h"
-#include "shared/ControlPanel.h"
 #include "shared/Updater.h"
-#include <thread>
 #include <d2d1.h>
 #include <dwrite.h>
 #include <string>
+#include <thread>
 
 #include <commdlg.h>
 #pragma comment(lib, "comdlg32.lib")
@@ -26,229 +26,336 @@
 #endif
 using namespace D2D1;
 
-ID2D1Factory* g_pD2DFactory = NULL;
-IDWriteFactory* g_pDWriteFactory = NULL;
-ID2D1HwndRenderTarget* g_pRenderTarget = NULL;
+ID2D1Factory *g_pD2DFactory = NULL;
+IDWriteFactory *g_pDWriteFactory = NULL;
+ID2D1HwndRenderTarget *g_pRenderTarget = NULL;
 
 // UI State
 // extern declared in State.h
 
 void InitD2D(HWND hWnd) {
-    if (!g_pD2DFactory) {
-        D2D1CreateFactory(D2D1_FACTORY_TYPE_SINGLE_THREADED, &g_pD2DFactory);
-        DWriteCreateFactory(DWRITE_FACTORY_TYPE_SHARED, __uuidof(IDWriteFactory), (IUnknown**)&g_pDWriteFactory);
-    }
-    RECT rc; GetClientRect(hWnd, &rc);
-    D2D1_SIZE_U size = D2D1::SizeU(rc.right - rc.left, rc.bottom - rc.top);
-    if (g_pRenderTarget) g_pRenderTarget->Release();
-    g_pD2DFactory->CreateHwndRenderTarget(D2D1::RenderTargetProperties(), D2D1::HwndRenderTargetProperties(hWnd, size), &g_pRenderTarget);
+  if (!g_pD2DFactory) {
+    D2D1CreateFactory(D2D1_FACTORY_TYPE_SINGLE_THREADED, &g_pD2DFactory);
+    DWriteCreateFactory(DWRITE_FACTORY_TYPE_SHARED, __uuidof(IDWriteFactory),
+                        (IUnknown **)&g_pDWriteFactory);
+  }
+  RECT rc;
+  GetClientRect(hWnd, &rc);
+  D2D1_SIZE_U size = D2D1::SizeU(rc.right - rc.left, rc.bottom - rc.top);
+  if (g_pRenderTarget)
+    g_pRenderTarget->Release();
+  g_pD2DFactory->CreateHwndRenderTarget(
+      D2D1::RenderTargetProperties(),
+      D2D1::HwndRenderTargetProperties(hWnd, size), &g_pRenderTarget);
 }
 
-void DrawD2DButton(ID2D1HwndRenderTarget* rt, D2D1_RECT_F rect, const wchar_t* text, D2D1_COLOR_F color) {
-    ID2D1SolidColorBrush* pBrush = NULL;
-    rt->CreateSolidColorBrush(color, &pBrush);
-    
-    rt->FillRoundedRectangle(D2D1::RoundedRect(rect, 8.0f, 8.0f), pBrush);
-    
-    ID2D1SolidColorBrush* pWhite = NULL;
-    rt->CreateSolidColorBrush(ColorF(ColorF::White), &pWhite);
-    
-    IDWriteTextFormat* pTextFormat = NULL;
-    g_pDWriteFactory->CreateTextFormat(L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 14.0f, L"en-us", &pTextFormat);
-    pTextFormat->SetTextAlignment(DWRITE_TEXT_ALIGNMENT_CENTER);
-    pTextFormat->SetParagraphAlignment(DWRITE_PARAGRAPH_ALIGNMENT_CENTER);
-    
-    rt->DrawText(text, (UINT32)wcslen(text), pTextFormat, rect, pWhite);
-    
-    pTextFormat->Release();
-    pWhite->Release();
-    pBrush->Release();
+void DrawD2DButton(ID2D1HwndRenderTarget *rt, D2D1_RECT_F rect,
+                   const wchar_t *text, D2D1_COLOR_F color) {
+  ID2D1SolidColorBrush *pBrush = NULL;
+  rt->CreateSolidColorBrush(color, &pBrush);
+
+  rt->FillRoundedRectangle(D2D1::RoundedRect(rect, 8.0f, 8.0f), pBrush);
+
+  ID2D1SolidColorBrush *pWhite = NULL;
+  rt->CreateSolidColorBrush(ColorF(ColorF::White), &pWhite);
+
+  IDWriteTextFormat *pTextFormat = NULL;
+  g_pDWriteFactory->CreateTextFormat(
+      L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_BOLD,
+      DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 14.0f, L"en-us",
+      &pTextFormat);
+  pTextFormat->SetTextAlignment(DWRITE_TEXT_ALIGNMENT_CENTER);
+  pTextFormat->SetParagraphAlignment(DWRITE_PARAGRAPH_ALIGNMENT_CENTER);
+
+  rt->DrawText(text, (UINT32)wcslen(text), pTextFormat, rect, pWhite);
+
+  pTextFormat->Release();
+  pWhite->Release();
+  pBrush->Release();
 }
 
 HWND CreateControlPanel(HINSTANCE hInst) {
-    WNDCLASS wc = { 0 };
-    wc.lpfnWndProc = ControlPanelWndProc;
-    wc.hInstance = hInst;
-    wc.hCursor = LoadCursor(NULL, IDC_ARROW);
-    wc.hbrBackground = (HBRUSH)(COLOR_WINDOW + 1);
-    wc.lpszClassName = L"BetterAngleControlPanel";
-    RegisterClass(&wc);
+  WNDCLASS wc = {0};
+  wc.lpfnWndProc = ControlPanelWndProc;
+  wc.hInstance = hInst;
+  wc.hCursor = LoadCursor(NULL, IDC_ARROW);
+  wc.hbrBackground = (HBRUSH)(COLOR_WINDOW + 1);
+  wc.lpszClassName = L"BetterAngleControlPanel";
+  RegisterClass(&wc);
 
-    int w = 420, h = 680;
-    HWND hPanel = CreateWindowEx(
-        WS_EX_TOPMOST | WS_EX_APPWINDOW,
-        L"BetterAngleControlPanel", L"BetterAngle Pro | Global Command Center",
-        WS_OVERLAPPED | WS_CAPTION | WS_SYSMENU | WS_MINIMIZEBOX,
-        CW_USEDEFAULT, CW_USEDEFAULT, w, h,
-        NULL, NULL, hInst, NULL
-    );
+  int w = 420, h = 680;
+  HWND hPanel = CreateWindowEx(
+      WS_EX_TOPMOST | WS_EX_APPWINDOW, L"BetterAngleControlPanel",
+      L"BetterAngle Pro | Global Command Center",
+      WS_OVERLAPPED | WS_CAPTION | WS_SYSMENU | WS_MINIMIZEBOX, CW_USEDEFAULT,
+      CW_USEDEFAULT, w, h, NULL, NULL, hInst, NULL);
 
-    ShowWindow(hPanel, SW_SHOW);
-    UpdateWindow(hPanel);
-    return hPanel;
+  ShowWindow(hPanel, SW_SHOW);
+  UpdateWindow(hPanel);
+  return hPanel;
 }
 
-LRESULT CALLBACK ControlPanelWndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam) {
-    switch (message) {
-        case WM_CREATE:
-            InitD2D(hWnd);
-            SetTimer(hWnd, 1, 16, NULL); // 60FPS Refresh Rate
-            return 0;
-        case WM_SIZE:
-            InitD2D(hWnd);
-            return 0;
-        case WM_LBUTTONDOWN: {
-            int x = LOWORD(lParam), y = HIWORD(lParam);
-            if (y >= 90 && y <= 120) {
-                if (x >= 40 && x <= 200) g_currentTab = 0;
-                else if (x >= 210 && x <= 380) g_currentTab = 1;
-            }
-            if (g_currentTab == 1) {
-                if (x >= 40 && x <= 380 && y >= 320 && y <= 370) {
-                    if (g_updateAvailable) {
-                        // FIX: Immediately lock the state to prevent multiple threads
-                        g_updateAvailable = false;
+LRESULT CALLBACK ControlPanelWndProc(HWND hWnd, UINT message, WPARAM wParam,
+                                     LPARAM lParam) {
+  switch (message) {
+  case WM_CREATE:
+    InitD2D(hWnd);
+    SetTimer(hWnd, 1, 16, NULL); // 60FPS Refresh Rate
+    return 0;
+  case WM_SIZE:
+    InitD2D(hWnd);
+    return 0;
+  case WM_LBUTTONDOWN: {
+    int x = LOWORD(lParam), y = HIWORD(lParam);
+    {
+      std::lock_guard<std::mutex> lock(g_stateMutex);
+      if (y >= 90 && y <= 120) {
+        if (x >= 40 && x <= 200)
+          g_currentTab = 0;
+        else if (x >= 210 && x <= 380)
+          g_currentTab = 1;
+      }
+      if (g_currentTab == 1) {
+        if (x >= 40 && x <= 380 && y >= 320 && y <= 370) {
+          if (g_updateAvailable) {
+            // FIX: Immediately lock the state to prevent multiple threads
+            g_updateAvailable = false;
 
-                        std::thread([]() {
-                            std::wstring verStr = std::wstring(g_latestVersionOnline.begin(), g_latestVersionOnline.end());
-                            std::wstring downloadUrl = L"https://github.com/MahanYTT/BetterAngle/releases/latest/download/BetterAngle.exe";
-                            if (DownloadUpdate(downloadUrl, L"update_tmp.exe")) {
-                                ApplyUpdateAndRestart();
-                            }
-                        }).detach();
-                    } else {
-                        g_isCheckingForUpdates = true;
-                        std::thread(CheckForUpdates).detach();
-                    }
-                }
-                if (g_updateAvailable && x >= 40 && x <= 380 && y >= 380 && y <= 430) {
-                    ShellExecuteW(0, L"open", L"https://github.com/MahanYTT/BetterAngle/releases/latest", 0, 0, SW_SHOW);
-                }
-                if (g_latestVersion > 4.92f && x >= 40 && x <= 380 && y >= 270 && y <= 290) {
-                    ShellExecuteW(0, L"open", L"https://github.com/MahanYTT/BetterAngle/releases", 0, 0, SW_SHOW);
-                }
-            }
+            std::thread([]() {
+              std::wstring verStr = std::wstring(g_latestVersionOnline.begin(),
+                                                 g_latestVersionOnline.end());
+              std::wstring downloadUrl =
+                  L"https://github.com/MahanYTT/BetterAngle/releases/latest/"
+                  L"download/BetterAngle.exe";
+              if (DownloadUpdate(downloadUrl, L"update_tmp.exe")) {
+                ApplyUpdateAndRestart();
+              }
+            }).detach();
+          } else {
+            g_isCheckingForUpdates = true;
+            std::thread(CheckForUpdates).detach();
+          }
+        }
+        if (g_updateAvailable && x >= 40 && x <= 380 && y >= 380 && y <= 430) {
+          ShellExecuteW(
+              0, L"open",
+              L"https://github.com/MahanYTT/BetterAngle/releases/latest", 0, 0,
+              SW_SHOW);
+        }
+        if (g_latestVersion > 4.92f && x >= 40 && x <= 380 && y >= 270 &&
+            y <= 290) {
+          ShellExecuteW(0, L"open",
+                        L"https://github.com/MahanYTT/BetterAngle/releases", 0,
+                        0, SW_SHOW);
+        }
+      }
 
-            if (x >= 40 && x <= 380 && y >= 580 && y <= 630) {
-                PostQuitMessage(0);
-            }
-            return 0;
-        } // <-- Make sure this closing brace exists!
-        case WM_PAINT: {
-            if (!g_pRenderTarget) return 0;
-            g_pRenderTarget->BeginDraw();
-            g_pRenderTarget->Clear(D2D1::ColorF(0.05f, 0.06f, 0.07f));
+      if (x >= 40 && x <= 380 && y >= 580 && y <= 630) {
+        PostQuitMessage(0);
+      }
+    }
+    return 0;
+  } // <-- Make sure this closing brace exists!
+  case WM_PAINT: {
+    if (!g_pRenderTarget)
+      return 0;
+    {
+      std::lock_guard<std::mutex> lock(g_stateMutex);
+      g_pRenderTarget->BeginDraw();
+      g_pRenderTarget->Clear(D2D1::ColorF(0.05f, 0.06f, 0.07f));
 
-            ID2D1SolidColorBrush* pWhite = NULL;
-            g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(D2D1::ColorF::White), &pWhite);
-            ID2D1SolidColorBrush* pGrey = NULL;
-            g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(0.6f, 0.6f, 0.6f), &pGrey);
-            ID2D1SolidColorBrush* pBlue = NULL;
-            g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(0.3f, 0.7f, 1.0f), &pBlue);
+      ID2D1SolidColorBrush *pWhite = NULL;
+      g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(D2D1::ColorF::White),
+                                             &pWhite);
+      ID2D1SolidColorBrush *pGrey = NULL;
+      g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(0.6f, 0.6f, 0.6f),
+                                             &pGrey);
+      ID2D1SolidColorBrush *pBlue = NULL;
+      g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(0.3f, 0.7f, 1.0f),
+                                             &pBlue);
 
-            IDWriteTextFormat* pTitleFormat = NULL;
-            g_pDWriteFactory->CreateTextFormat(L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 24.0f, L"en-us", &pTitleFormat);
-            IDWriteTextFormat* pHeaderFormat = NULL;
-            g_pDWriteFactory->CreateTextFormat(L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 16.0f, L"en-us", &pHeaderFormat);
-            IDWriteTextFormat* pVerFormat = NULL;
-            g_pDWriteFactory->CreateTextFormat(L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_REGULAR, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 13.0f, L"en-us", &pVerFormat);
+      IDWriteTextFormat *pTitleFormat = NULL;
+      g_pDWriteFactory->CreateTextFormat(
+          L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_BOLD,
+          DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 24.0f, L"en-us",
+          &pTitleFormat);
+      IDWriteTextFormat *pHeaderFormat = NULL;
+      g_pDWriteFactory->CreateTextFormat(
+          L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_BOLD,
+          DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 16.0f, L"en-us",
+          &pHeaderFormat);
+      IDWriteTextFormat *pVerFormat = NULL;
+      g_pDWriteFactory->CreateTextFormat(
+          L"Segoe UI Variable Display", NULL, DWRITE_FONT_WEIGHT_REGULAR,
+          DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, 13.0f, L"en-us",
+          &pVerFormat);
 
-            g_pRenderTarget->DrawText(L"Pro Command Center", 18, pTitleFormat, D2D1::RectF(40, 40, 380, 80), pWhite);
-            
-            DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 90, 200, 120), L"GENERAL / BINDS", g_currentTab == 0 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));
-            DrawD2DButton(g_pRenderTarget, D2D1::RectF(210, 90, 380, 120), L"UPDATES", g_currentTab == 1 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));
+      g_pRenderTarget->DrawText(L"Pro Command Center", 18, pTitleFormat,
+                                D2D1::RectF(40, 40, 380, 80), pWhite);
+
+      DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 90, 200, 120),
+                    L"GENERAL / BINDS",
+                    g_currentTab == 0 ? D2D1::ColorF(0.2f, 0.25f, 0.3f)
+                                      : D2D1::ColorF(0.1f, 0.12f, 0.15f));
+      DrawD2DButton(g_pRenderTarget, D2D1::RectF(210, 90, 380, 120), L"UPDATES",
+                    g_currentTab == 1 ? D2D1::ColorF(0.2f, 0.25f, 0.3f)
+                                      : D2D1::ColorF(0.1f, 0.12f, 0.15f));
 
-            if (g_currentTab == 0) {
-                g_pRenderTarget->DrawText(L"CURRENT KEYBINDS", 16, pHeaderFormat, D2D1::RectF(40, 140, 380, 170), pWhite);
-                
-                // Target Color Preview Circle (Linked to g_targetColor)
-                ID2D1SolidColorBrush* pTargetBrush = NULL;
-                g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(GetRValue(g_targetColor)/255.0f, GetGValue(g_targetColor)/255.0f, GetBValue(g_targetColor)/255.0f), &pTargetBrush);
-                g_pRenderTarget->FillEllipse(D2D1::Ellipse(D2D1::Point2F(360.0f, 155.0f), 12.0f, 12.0f), pTargetBrush);
-                g_pRenderTarget->DrawEllipse(D2D1::Ellipse(D2D1::Point2F(360.0f, 155.0f), 12.0f, 12.0f), pGrey, 1.0f);
-                if (pTargetBrush) pTargetBrush->Release();
+      if (g_currentTab == 0) {
+        g_pRenderTarget->DrawText(L"CURRENT KEYBINDS", 16, pHeaderFormat,
+                                  D2D1::RectF(40, 140, 380, 170), pWhite);
+
+        // Target Color Preview Circle (Linked to g_targetColor)
+        ID2D1SolidColorBrush *pTargetBrush = NULL;
+        g_pRenderTarget->CreateSolidColorBrush(
+            D2D1::ColorF(GetRValue(g_targetColor) / 255.0f,
+                         GetGValue(g_targetColor) / 255.0f,
+                         GetBValue(g_targetColor) / 255.0f),
+            &pTargetBrush);
+        g_pRenderTarget->FillEllipse(
+            D2D1::Ellipse(D2D1::Point2F(360.0f, 155.0f), 12.0f, 12.0f),
+            pTargetBrush);
+        g_pRenderTarget->DrawEllipse(
+            D2D1::Ellipse(D2D1::Point2F(360.0f, 155.0f), 12.0f, 12.0f), pGrey,
+            1.0f);
+        if (pTargetBrush)
+          pTargetBrush->Release();
 
-                g_pRenderTarget->DrawText(L"Precision Crosshair: F10\nVisual ROI Selector: Ctrl + R\nToggle ROI Box: F9", 66, pVerFormat, D2D1::RectF(40, 170, 380, 240), pGrey);
-                // Quick Guide for Workspace selection (v4.9.20: Removed hidden color picker)
-                g_pRenderTarget->DrawText(L"Press CTRL+R to begin full-screen selection.", 45, pVerFormat, D2D1::RectF(40, 250, 380, 290), pWhite);
+        g_pRenderTarget->DrawText(L"Precision Crosshair: F10\nVisual ROI "
+                                  L"Selector: Ctrl + R\nToggle ROI Box: F9",
+                                  66, pVerFormat,
+                                  D2D1::RectF(40, 170, 380, 240), pGrey);
+        // Quick Guide for Workspace selection (v4.9.20: Removed hidden color
+        // picker)
+        g_pRenderTarget->DrawText(
+            L"Press CTRL+R to begin full-screen selection.", 45, pVerFormat,
+            D2D1::RectF(40, 250, 380, 290), pWhite);
 
-            } else if (g_currentTab == 1) {
-                g_pRenderTarget->DrawText(L"SOFTWARE DASHBOARD", 18, pHeaderFormat, D2D1::RectF(40, 140, 380, 170), pWhite);
-                
-                if (g_isCheckingForUpdates) {
-                    D2D1_POINT_2F center = D2D1::Point2F(365.0f, 155.0f);
-                    g_pRenderTarget->SetTransform(D2D1::Matrix3x2F::Rotation(g_updateSpinAngle, center));
-                    g_pRenderTarget->DrawEllipse(D2D1::Ellipse(center, 6.0f, 6.0f), pBlue, 2.0f);
-                    g_pRenderTarget->SetTransform(D2D1::Matrix3x2F::Identity());
-                }
+      } else if (g_currentTab == 1) {
+        g_pRenderTarget->DrawText(L"SOFTWARE DASHBOARD", 18, pHeaderFormat,
+                                  D2D1::RectF(40, 140, 380, 170), pWhite);
+
+        if (g_isCheckingForUpdates) {
+          D2D1_POINT_2F center = D2D1::Point2F(365.0f, 155.0f);
+          g_pRenderTarget->SetTransform(
+              D2D1::Matrix3x2F::Rotation(g_updateSpinAngle, center));
+          g_pRenderTarget->DrawEllipse(D2D1::Ellipse(center, 6.0f, 6.0f), pBlue,
+                                       2.0f);
+          g_pRenderTarget->SetTransform(D2D1::Matrix3x2F::Identity());
+        }
 
-                std::wstring curVer = L"Current Version: v" APP_VERSION_WSTR L" (Live Pro Build)";
-                
-                // Use the real version string fetched from GitHub
-                std::wstring latestVerStr = std::wstring(g_latestVersionOnline.begin(), g_latestVersionOnline.end());
-                std::wstring latestVer = L"Latest Found: " + latestVerStr + L" (" + g_latestName + L")";
+        std::wstring curVer =
+            L"Current Version: v" APP_VERSION_WSTR L" (Live Pro Build)";
+
+        // Use the real version string fetched from GitHub
+        std::wstring latestVerStr = std::wstring(g_latestVersionOnline.begin(),
+                                                 g_latestVersionOnline.end());
+        std::wstring latestVer =
+            L"Latest Found: " + latestVerStr + L" (" + g_latestName + L")";
 
-                g_pRenderTarget->DrawText(curVer.c_str(), (UINT32)curVer.length(), pVerFormat, D2D1::RectF(40, 170, 380, 190), pGrey);
-                g_pRenderTarget->DrawText(latestVer.c_str(), (UINT32)latestVer.length(), pVerFormat, D2D1::RectF(40, 195, 380, 215), pGrey);
+        g_pRenderTarget->DrawText(curVer.c_str(), (UINT32)curVer.length(),
+                                  pVerFormat, D2D1::RectF(40, 170, 380, 190),
+                                  pGrey);
+        g_pRenderTarget->DrawText(latestVer.c_str(), (UINT32)latestVer.length(),
+                                  pVerFormat, D2D1::RectF(40, 195, 380, 215),
+                                  pGrey);
 
-                // Ensure it requires a version higher than 4.9.2 to trigger future updates
-                if (g_updateAvailable) {
-                    std::wstring changelog = L"BetterAngle v" APP_VERSION_WSTR L" is now available online!\nNewest Stable Version.";
-                    g_pRenderTarget->DrawText(changelog.c_str(), (UINT32)changelog.length(), pVerFormat, D2D1::RectF(40, 230, 380, 260), pWhite);
-                    std::wstring viewFull = L"View Full Changelog ->";
-                    g_pRenderTarget->DrawText(viewFull.c_str(), (UINT32)viewFull.length(), pVerFormat, D2D1::RectF(40, 270, 380, 290), pBlue);
-                    DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 380, 380, 430), L"DOWNLOAD UPDATE", D2D1::ColorF(0.0f, 0.5f, 0.8f));
-                }
-                DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 320, 380, 370), g_updateAvailable ? L"INSTALL UPDATE NOW" : L"DOWNLOAD AND INSTALL NOW", D2D1::ColorF(0.15f, 0.17f, 0.2f));
-            }
-            DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 90, 145, 120), L"GENERAL", g_currentTab == 0 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));
-            DrawD2DButton(g_pRenderTarget, D2D1::RectF(155, 90, 265, 120), L"UPDATES", g_currentTab == 1 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));
-            DrawD2DButton(g_pRenderTarget, D2D1::RectF(275, 90, 380, 120), L"COLORS", g_currentTab == 2 ? D2D1::ColorF(0.2f, 0.25f, 0.3f) : D2D1::ColorF(0.1f, 0.12f, 0.15f));
-            if (g_currentTab == 2) {
-                g_pRenderTarget->DrawText(L"COLOR CONFIGURATION", 19, pHeaderFormat, D2D1::RectF(40, 140, 380, 170), pWhite);
+        // Ensure it requires a version higher than 4.9.2 to trigger future
+        // updates
+        if (g_updateAvailable) {
+          std::wstring changelog =
+              L"BetterAngle "
+              L"v" APP_VERSION_WSTR L" is now available online!\nNewest Stable "
+              L"Version.";
+          g_pRenderTarget->DrawText(changelog.c_str(),
+                                    (UINT32)changelog.length(), pVerFormat,
+                                    D2D1::RectF(40, 230, 380, 260), pWhite);
+          std::wstring viewFull = L"View Full Changelog ->";
+          g_pRenderTarget->DrawText(viewFull.c_str(), (UINT32)viewFull.length(),
+                                    pVerFormat, D2D1::RectF(40, 270, 380, 290),
+                                    pBlue);
+          DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 380, 380, 430),
+                        L"DOWNLOAD UPDATE", D2D1::ColorF(0.0f, 0.5f, 0.8f));
+        }
+        DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 320, 380, 370),
+                      g_updateAvailable ? L"INSTALL UPDATE NOW"
+                                        : L"DOWNLOAD AND INSTALL NOW",
+                      D2D1::ColorF(0.15f, 0.17f, 0.2f));
+      }
+      DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 90, 145, 120), L"GENERAL",
+                    g_currentTab == 0 ? D2D1::ColorF(0.2f, 0.25f, 0.3f)
+                                      : D2D1::ColorF(0.1f, 0.12f, 0.15f));
+      DrawD2DButton(g_pRenderTarget, D2D1::RectF(155, 90, 265, 120), L"UPDATES",
+                    g_currentTab == 1 ? D2D1::ColorF(0.2f, 0.25f, 0.3f)
+                                      : D2D1::ColorF(0.1f, 0.12f, 0.15f));
+      DrawD2DButton(g_pRenderTarget, D2D1::RectF(275, 90, 380, 120), L"COLORS",
+                    g_currentTab == 2 ? D2D1::ColorF(0.2f, 0.25f, 0.3f)
+                                      : D2D1::ColorF(0.1f, 0.12f, 0.15f));
+      if (g_currentTab == 2) {
+        g_pRenderTarget->DrawText(L"COLOR CONFIGURATION", 19, pHeaderFormat,
+                                  D2D1::RectF(40, 140, 380, 170), pWhite);
 
-                // Preview of Active Color
-                g_pRenderTarget->DrawText(L"Active Target Color:", 20, pVerFormat, D2D1::RectF(40, 180, 250, 200), pGrey);
+        // Preview of Active Color
+        g_pRenderTarget->DrawText(L"Active Target Color:", 20, pVerFormat,
+                                  D2D1::RectF(40, 180, 250, 200), pGrey);
 
-                ID2D1SolidColorBrush* pPreviewBrush = NULL;
-                g_pRenderTarget->CreateSolidColorBrush(D2D1::ColorF(GetRValue(g_targetColor)/255.0f, GetGValue(g_targetColor)/255.0f, GetBValue(g_targetColor)/255.0f), &pPreviewBrush);
-                g_pRenderTarget->FillRoundedRectangle(D2D1::RoundedRect(D2D1::RectF(260, 175, 380, 205), 4.0f, 4.0f), pPreviewBrush);
-                if (pPreviewBrush) pPreviewBrush->Release();
+        ID2D1SolidColorBrush *pPreviewBrush = NULL;
+        g_pRenderTarget->CreateSolidColorBrush(
+            D2D1::ColorF(GetRValue(g_targetColor) / 255.0f,
+                         GetGValue(g_targetColor) / 255.0f,
+                         GetBValue(g_targetColor) / 255.0f),
+            &pPreviewBrush);
+        g_pRenderTarget->FillRoundedRectangle(
+            D2D1::RoundedRect(D2D1::RectF(260, 175, 380, 205), 4.0f, 4.0f),
+            pPreviewBrush);
+        if (pPreviewBrush)
+          pPreviewBrush->Release();
 
-                // Instructions
-                std::wstring colorDesc = L"The algorithm focuses on this specific color value to calculate angles. Accuracy depends on your monitor's color profile.";
-                g_pRenderTarget->DrawText(colorDesc.c_str(), (UINT32)colorDesc.length(), pVerFormat, D2D1::RectF(40, 220, 380, 280), pGrey);
+        // Instructions
+        std::wstring colorDesc =
+            L"The algorithm focuses on this specific color value to calculate "
+            L"angles. Accuracy depends on your monitor's color profile.";
+        g_pRenderTarget->DrawText(colorDesc.c_str(), (UINT32)colorDesc.length(),
+                                  pVerFormat, D2D1::RectF(40, 220, 380, 280),
+                                  pGrey);
 
-                // Interaction Button
-                DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 320, 380, 370), L"OPEN COLOR PALETTE", D2D1::ColorF(0.2f, 0.4f, 0.6f));
-            }
+        // Interaction Button
+        DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 320, 380, 370),
+                      L"OPEN COLOR PALETTE", D2D1::ColorF(0.2f, 0.4f, 0.6f));
+      }
 
-            DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 580, 380, 630), L"QUIT SUITE", D2D1::ColorF(0.7f, 0.1f, 0.15f));
+      DrawD2DButton(g_pRenderTarget, D2D1::RectF(40, 580, 380, 630),
+                    L"QUIT SUITE", D2D1::ColorF(0.7f, 0.1f, 0.15f));
 
-            pVerFormat->Release();
-            pHeaderFormat->Release();
-            pTitleFormat->Release();
-            pBlue->Release();
-            pGrey->Release();
-            pWhite->Release();
-            g_pRenderTarget->EndDraw();
-            ValidateRect(hWnd, NULL);
-            return 0;
-        }
-        case WM_TIMER:
-            if (g_isCheckingForUpdates) {
-                g_updateSpinAngle += 10.0f;
-                if (g_updateSpinAngle >= 360.0f) g_updateSpinAngle = 0.0f;
-            }
-            InvalidateRect(hWnd, NULL, FALSE);
-            return 0;
-        case WM_CLOSE:
-            ShowWindow(hWnd, SW_MINIMIZE);
-            return 0;
-        case WM_DESTROY:
-            if (g_pRenderTarget) g_pRenderTarget->Release();
-            return 0;
-        default:
-            return DefWindowProc(hWnd, message, wParam, lParam);
-    }
+      pVerFormat->Release();
+      pHeaderFormat->Release();
+      pTitleFormat->Release();
+      pBlue->Release();
+      pGrey->Release();
+      pWhite->Release();
+      g_pRenderTarget->EndDraw();
+    }
+    ValidateRect(hWnd, NULL);
+    return 0;
+  }
+  case WM_TIMER: {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
+    if (g_isCheckingForUpdates) {
+      g_updateSpinAngle += 10.0f;
+      if (g_updateSpinAngle >= 360.0f)
+        g_updateSpinAngle = 0.0f;
+    }
+  }
+    InvalidateRect(hWnd, NULL, FALSE);
+    return 0;
+  case WM_CLOSE:
+    ShowWindow(hWnd, SW_MINIMIZE);
+    return 0;
+  case WM_DESTROY:
+    if (g_pRenderTarget)
+      g_pRenderTarget->Release();
+    return 0;
+  default:
+    return DefWindowProc(hWnd, message, wParam, lParam);
+  }
 }
Index: src/shared/Logic.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"shared/Logic.h\"\r\n#include <cmath>\r\n#include \"shared/State.h\"\r\n\r\nbool IsFortniteFocused() {\r\n    HWND hForeground = GetForegroundWindow();\r\n    if (!hForeground) return false;\r\n\r\n    wchar_t className[256];\r\n    GetClassNameW(hForeground, className, 256);\r\n    std::wstring cls(className);\r\n\r\n    // Fortnite uses \"UnrealWindow\" or \"Fortnite\"\r\n    if (cls == L\"UnrealWindow\" || cls == L\"Fortnite\") {\r\n        return true;\r\n    }\r\n\r\n    wchar_t title[256];\r\n    GetWindowTextW(hForeground, title, 256);\r\n    std::wstring wTitle(title);\r\n\r\n    return (wTitle.find(L\"Fortnite\") != std::wstring::npos);\r\n}\r\n\r\nAngleLogic::AngleLogic(double dpi, double sens)\r\n    : m_dpi(dpi), m_sens(sens), m_accumDx(0), m_baseDx(0), m_baseAngle(0.0), m_scalePerDx(0.0) {}\r\n\r\nvoid AngleLogic::Update(int dx) {\r\n    if (!g_debugMode && !IsFortniteFocused()) return;\r\n    m_accumDx += dx;\r\n}\r\n\r\ndouble AngleLogic::GetAngle() const {\r\n    if (m_scalePerDx == 0.0) return 0.0;\r\n    double angle = Norm360(m_baseAngle + (m_accumDx - m_baseDx) * m_scalePerDx);\r\n    \r\n    // Heuristic Check (v4.9.15)\r\n    g_currentAngle = (float)angle;\r\n    if (g_currentSelection == NONE) {\r\n        if (g_currentAngle > 50.0f || g_currentAngle < -50.0f) {\r\n            g_isDiving = true;\r\n        } else {\r\n            g_isDiving = false;\r\n        }\r\n    }\r\n    \r\n    return angle;\r\n}\r\n\r\nvoid AngleLogic::SetZero() {\r\n    m_baseAngle = 0.0;\r\n    m_baseDx = m_accumDx;\r\n}\r\n\r\nvoid AngleLogic::SetScale(double scale) {\r\n    m_scalePerDx = scale;\r\n}\r\n\r\ndouble AngleLogic::Norm360(double a) const {\r\n    double res = fmod(a, 360.0);\r\n    if (res < 0) res += 360.0;\r\n    return res;\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/shared/Logic.cpp b/src/shared/Logic.cpp
--- a/src/shared/Logic.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/shared/Logic.cpp	(date 1775748098235)
@@ -1,63 +1,70 @@
 #include "shared/Logic.h"
-#include <cmath>
 #include "shared/State.h"
+#include <cmath>
+
 
 bool IsFortniteFocused() {
-    HWND hForeground = GetForegroundWindow();
-    if (!hForeground) return false;
+  HWND hForeground = GetForegroundWindow();
+  if (!hForeground)
+    return false;
 
-    wchar_t className[256];
-    GetClassNameW(hForeground, className, 256);
-    std::wstring cls(className);
+  wchar_t className[256];
+  GetClassNameW(hForeground, className, 256);
+  std::wstring cls(className);
 
-    // Fortnite uses "UnrealWindow" or "Fortnite"
-    if (cls == L"UnrealWindow" || cls == L"Fortnite") {
-        return true;
-    }
+  // Fortnite uses "UnrealWindow" or "Fortnite"
+  if (cls == L"UnrealWindow" || cls == L"Fortnite") {
+    return true;
+  }
 
-    wchar_t title[256];
-    GetWindowTextW(hForeground, title, 256);
-    std::wstring wTitle(title);
+  wchar_t title[256];
+  GetWindowTextW(hForeground, title, 256);
+  std::wstring wTitle(title);
 
-    return (wTitle.find(L"Fortnite") != std::wstring::npos);
+  return (wTitle.find(L"Fortnite") != std::wstring::npos);
 }
 
 AngleLogic::AngleLogic(double dpi, double sens)
-    : m_dpi(dpi), m_sens(sens), m_accumDx(0), m_baseDx(0), m_baseAngle(0.0), m_scalePerDx(0.0) {}
+    : m_dpi(dpi), m_sens(sens), m_accumDx(0), m_baseDx(0), m_baseAngle(0.0),
+      m_scalePerDx(0.0) {}
 
 void AngleLogic::Update(int dx) {
-    if (!g_debugMode && !IsFortniteFocused()) return;
-    m_accumDx += dx;
+  if (!g_debugMode && !IsFortniteFocused())
+    return;
+  m_accumDx += dx;
 }
 
 double AngleLogic::GetAngle() const {
-    if (m_scalePerDx == 0.0) return 0.0;
-    double angle = Norm360(m_baseAngle + (m_accumDx - m_baseDx) * m_scalePerDx);
-    
-    // Heuristic Check (v4.9.15)
+  if (m_scalePerDx == 0.0)
+    return 0.0;
+  double angle = Norm360(m_baseAngle + (m_accumDx - m_baseDx) * m_scalePerDx);
+
+  // Heuristic Check (v4.9.15)
+  {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
     g_currentAngle = (float)angle;
     if (g_currentSelection == NONE) {
-        if (g_currentAngle > 50.0f || g_currentAngle < -50.0f) {
-            g_isDiving = true;
-        } else {
-            g_isDiving = false;
-        }
+      if (g_currentAngle > 50.0f || g_currentAngle < -50.0f) {
+        g_isDiving = true;
+      } else {
+        g_isDiving = false;
+      }
     }
-    
-    return angle;
+  }
+
+  return angle;
 }
 
 void AngleLogic::SetZero() {
-    m_baseAngle = 0.0;
-    m_baseDx = m_accumDx;
+  m_baseAngle = 0.0;
+  m_baseDx = m_accumDx;
 }
 
-void AngleLogic::SetScale(double scale) {
-    m_scalePerDx = scale;
-}
+void AngleLogic::SetScale(double scale) { m_scalePerDx = scale; }
 
 double AngleLogic::Norm360(double a) const {
-    double res = fmod(a, 360.0);
-    if (res < 0) res += 360.0;
-    return res;
+  double res = fmod(a, 360.0);
+  if (res < 0)
+    res += 360.0;
+  return res;
 }
Index: .idea/workspace.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+><?xml version=\"1.0\" encoding=\"UTF-8\"?>\r\n<project version=\"4\">\r\n  <component name=\"AutoImportSettings\">\r\n    <option name=\"autoReloadType\" value=\"SELECTIVE\" />\r\n  </component>\r\n  <component name=\"BackendCodeEditorMiscSettings\">\r\n    <option name=\"/Default/Housekeeping/FeatureSuggestion/FeatureSuggestionManager/DisabledSuggesters/=SwitchToGoToActionSuggester/@EntryIndexedValue\" value=\"true\" type=\"bool\" />\r\n    <option name=\"/Default/Housekeeping/GlobalSettingsUpgraded/IsUpgraded/@EntryValue\" value=\"true\" type=\"bool\" />\r\n    <option name=\"/Default/RiderDebugger/RiderRestoreDecompile/RestoreDecompileSetting/@EntryValue\" value=\"false\" type=\"bool\" />\r\n  </component>\r\n  <component name=\"CMakeProjectFlavorService\">\r\n    <option name=\"flavorId\" value=\"CMakePlainProjectFlavor\" />\r\n  </component>\r\n  <component name=\"CMakeSettings\">\r\n    <configurations>\r\n      <configuration PROFILE_NAME=\"Debug\" ENABLED=\"true\" CONFIG_NAME=\"Debug\" />\r\n      <configuration PROFILE_NAME=\"Release\" ENABLED=\"true\" CONFIG_NAME=\"Release\" />\r\n    </configurations>\r\n  </component>\r\n  <component name=\"ChangeListManager\">\r\n    <list default=\"true\" id=\"ded3ab96-8c88-4192-9044-339706012801\" name=\"Changes\" comment=\"\">\r\n      <change beforePath=\"$PROJECT_DIR$/.idea/workspace.xml\" beforeDir=\"false\" afterPath=\"$PROJECT_DIR$/.idea/workspace.xml\" afterDir=\"false\" />\r\n      <change beforePath=\"$PROJECT_DIR$/src/shared/Updater.cpp\" beforeDir=\"false\" afterPath=\"$PROJECT_DIR$/src/shared/Updater.cpp\" afterDir=\"false\" />\r\n    </list>\r\n    <option name=\"SHOW_DIALOG\" value=\"false\" />\r\n    <option name=\"HIGHLIGHT_CONFLICTS\" value=\"true\" />\r\n    <option name=\"HIGHLIGHT_NON_ACTIVE_CHANGELIST\" value=\"false\" />\r\n    <option name=\"LAST_RESOLUTION\" value=\"IGNORE\" />\r\n  </component>\r\n  <component name=\"ClangdSettings\">\r\n    <option name=\"formatViaClangd\" value=\"false\" />\r\n  </component>\r\n  <component name=\"EmbeddingIndexingInfo\">\r\n    <option name=\"cachedIndexableFilesCount\" value=\"40\" />\r\n    <option name=\"fileBasedEmbeddingIndicesEnabled\" value=\"true\" />\r\n  </component>\r\n  <component name=\"Git.Settings\">\r\n    <option name=\"RECENT_GIT_ROOT_PATH\" value=\"$PROJECT_DIR$\" />\r\n  </component>\r\n  <component name=\"HighlightingSettingsPerFile\">\r\n    <setting file=\"file://$PROJECT_DIR$/include/shared/Config.h\" root0=\"FORCE_HIGHLIGHTING\" />\r\n    <setting file=\"file://$PROJECT_DIR$/src/main_app/BetterAngle.cpp\" root0=\"FORCE_HIGHLIGHTING\" />\r\n    <setting file=\"file://$PROJECT_DIR$/src/shared/Config.cpp\" root0=\"FORCE_HIGHLIGHTING\" />\r\n    <setting file=\"file://$PROJECT_DIR$/src/shared/ControlPanel.cpp\" root0=\"FORCE_HIGHLIGHTING\" />\r\n  </component>\r\n  <component name=\"MacroExpansionManager\">\r\n    <option name=\"directoryName\" value=\"updwjVeV\" />\r\n  </component>\r\n  <component name=\"McpProjectServerCommands\">\r\n    <commands />\r\n    <urls />\r\n  </component>\r\n  <component name=\"ProjectColorInfo\">{\r\n  &quot;associatedIndex&quot;: 3\r\n}</component>\r\n  <component name=\"ProjectId\" id=\"3C4kEDdoEFkFkgr9SPQYGANcHzt\" />\r\n  <component name=\"ProjectStatusUIService\">{\r\n  &quot;uiKindId&quot;: &quot;Widget&quot;\r\n}</component>\r\n  <component name=\"ProjectViewState\">\r\n    <option name=\"hideEmptyMiddlePackages\" value=\"true\" />\r\n    <option name=\"showLibraryContents\" value=\"true\" />\r\n  </component>\r\n  <component name=\"PropertiesComponent\">{\r\n  &quot;keyToString&quot;: {\r\n    &quot;ASKED_MARK_IGNORED_FILES_AS_EXCLUDED&quot;: &quot;true&quot;,\r\n    &quot;C/C++ File.BetterAngle.cpp.executor&quot;: &quot;Run&quot;,\r\n    &quot;Custom Build Application.Main.executor&quot;: &quot;Run&quot;,\r\n    &quot;ModuleVcsDetector.initialDetectionPerformed&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.MCP Project settings loaded&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.RadMigrateCodeStyle&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.ShowReadmeOnStart&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.TerminalTabsStorage.copyFrom.TerminalArrangementManager.252&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.cidr.known.project.marker&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.git.unshallow&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.readMode.enableVisualFormatting&quot;: &quot;true&quot;,\r\n    &quot;RunOnceActivity.typescript.service.memoryLimit.init&quot;: &quot;true&quot;,\r\n    &quot;Shell Script.Build with Batch Script.executor&quot;: &quot;Run&quot;,\r\n    &quot;Shell Script.Build with MSVC Tools.executor&quot;: &quot;Run&quot;,\r\n    &quot;cf.first.check.clang-format&quot;: &quot;false&quot;,\r\n    &quot;cidr.known.project.marker&quot;: &quot;true&quot;,\r\n    &quot;com.intellij.ml.llm.matterhorn.ej.ui.settings.DefaultModelSelectionForGA.v1&quot;: &quot;true&quot;,\r\n    &quot;git-widget-placeholder&quot;: &quot;main&quot;,\r\n    &quot;ignore.virus.scanning.warn.message&quot;: &quot;true&quot;,\r\n    &quot;junie.onboarding.icon.badge.shown&quot;: &quot;true&quot;,\r\n    &quot;last_opened_file_path&quot;: &quot;C:/Users/Brandon0/Desktop/dropmapreviewing/BetterAngle-GH&quot;,\r\n    &quot;node.js.detected.package.eslint&quot;: &quot;true&quot;,\r\n    &quot;node.js.detected.package.tslint&quot;: &quot;true&quot;,\r\n    &quot;node.js.selected.package.eslint&quot;: &quot;(autodetect)&quot;,\r\n    &quot;node.js.selected.package.tslint&quot;: &quot;(autodetect)&quot;,\r\n    &quot;nodejs_package_manager_path&quot;: &quot;npm&quot;,\r\n    &quot;org.rust.first.attach.projects&quot;: &quot;true&quot;,\r\n    &quot;settings.editor.selected.configurable&quot;: &quot;CPPToolchains&quot;,\r\n    &quot;to.speed.mode.migration.done&quot;: &quot;true&quot;,\r\n    &quot;vue.rearranger.settings.migration&quot;: &quot;true&quot;\r\n  }\r\n}</component>\r\n  <component name=\"RecapSpentCounter\">\r\n    <option name=\"endsOfQuotaMs\" value=\"1776614410539\" />\r\n    <option name=\"spentUsd\" value=\"0.081139795\" />\r\n  </component>\r\n  <component name=\"RecapUselessUpdatesCounter\">\r\n    <option name=\"suspendCountdown\" value=\"0\" />\r\n  </component>\r\n  <component name=\"RunManager\">\r\n    <configuration name=\"Build with MSVC Tools\" type=\"ShConfigurationType\">\r\n      <option name=\"SCRIPT_TEXT\" value=\"\" />\r\n      <option name=\"INDEPENDENT_SCRIPT_PATH\" value=\"false\" />\r\n      <option name=\"SCRIPT_PATH\" value=\"$PROJECT_DIR$/build.bat\" />\r\n      <option name=\"SCRIPT_OPTIONS\" value=\"\" />\r\n      <option name=\"INDEPENDENT_SCRIPT_WORKING_DIRECTORY\" value=\"true\" />\r\n      <option name=\"SCRIPT_WORKING_DIRECTORY\" value=\"\" />\r\n      <option name=\"INDEPENDENT_INTERPRETER_PATH\" value=\"true\" />\r\n      <option name=\"INTERPRETER_PATH\" value=\"cmd.exe\" />\r\n      <option name=\"INTERPRETER_OPTIONS\" value=\"/k &quot;&quot;C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\Common7\\Tools\\VsDevCmd.bat&quot;&quot;\" />\r\n      <option name=\"EXECUTE_IN_TERMINAL\" value=\"true\" />\r\n      <option name=\"EXECUTE_SCRIPT_FILE\" value=\"true\" />\r\n      <envs />\r\n      <method v=\"2\" />\r\n    </configuration>\r\n  </component>\r\n  <component name=\"TaskManager\">\r\n    <task active=\"true\" id=\"Default\" summary=\"Default task\">\r\n      <changelist id=\"ded3ab96-8c88-4192-9044-339706012801\" name=\"Changes\" comment=\"\" />\r\n      <created>1775652116213</created>\r\n      <option name=\"number\" value=\"Default\" />\r\n      <option name=\"presentableId\" value=\"Default\" />\r\n      <updated>1775652116213</updated>\r\n      <workItem from=\"1775652117485\" duration=\"556000\" />\r\n      <workItem from=\"1775653508326\" duration=\"220000\" />\r\n      <workItem from=\"1775653758776\" duration=\"2114000\" />\r\n      <workItem from=\"1775655893560\" duration=\"899000\" />\r\n      <workItem from=\"1775656860147\" duration=\"1022000\" />\r\n      <workItem from=\"1775660856789\" duration=\"14832000\" />\r\n      <workItem from=\"1775747915768\" duration=\"142000\" />\r\n    </task>\r\n    <servers />\r\n  </component>\r\n  <component name=\"TypeScriptGeneratedFilesManager\">\r\n    <option name=\"version\" value=\"3\" />\r\n  </component>\r\n</project>
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/workspace.xml b/.idea/workspace.xml
--- a/.idea/workspace.xml	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/.idea/workspace.xml	(date 1775748871178)
@@ -19,7 +19,17 @@
   </component>
   <component name="ChangeListManager">
     <list default="true" id="ded3ab96-8c88-4192-9044-339706012801" name="Changes" comment="">
-      <change beforePath="$PROJECT_DIR$/.idea/workspace.xml" beforeDir="false" afterPath="$PROJECT_DIR$/.idea/workspace.xml" afterDir="false" />
+      <change afterPath="$PROJECT_DIR$/CHANGELOG.md" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/build.bat" beforeDir="false" afterPath="$PROJECT_DIR$/build.bat" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/include/shared/Detector.h" beforeDir="false" afterPath="$PROJECT_DIR$/include/shared/Detector.h" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/include/shared/State.h" beforeDir="false" afterPath="$PROJECT_DIR$/include/shared/State.h" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/src/main_app/BetterAngle.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/main_app/BetterAngle.cpp" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/src/shared/ControlPanel.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/shared/ControlPanel.cpp" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/src/shared/Detector.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/shared/Detector.cpp" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/src/shared/Input.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/shared/Input.cpp" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/src/shared/Logic.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/shared/Logic.cpp" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/src/shared/Overlay.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/shared/Overlay.cpp" afterDir="false" />
+      <change beforePath="$PROJECT_DIR$/src/shared/State.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/shared/State.cpp" afterDir="false" />
       <change beforePath="$PROJECT_DIR$/src/shared/Updater.cpp" beforeDir="false" afterPath="$PROJECT_DIR$/src/shared/Updater.cpp" afterDir="false" />
     </list>
     <option name="SHOW_DIALOG" value="false" />
@@ -61,40 +71,40 @@
     <option name="hideEmptyMiddlePackages" value="true" />
     <option name="showLibraryContents" value="true" />
   </component>
-  <component name="PropertiesComponent">{
-  &quot;keyToString&quot;: {
-    &quot;ASKED_MARK_IGNORED_FILES_AS_EXCLUDED&quot;: &quot;true&quot;,
-    &quot;C/C++ File.BetterAngle.cpp.executor&quot;: &quot;Run&quot;,
-    &quot;Custom Build Application.Main.executor&quot;: &quot;Run&quot;,
-    &quot;ModuleVcsDetector.initialDetectionPerformed&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.MCP Project settings loaded&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.RadMigrateCodeStyle&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.ShowReadmeOnStart&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.TerminalTabsStorage.copyFrom.TerminalArrangementManager.252&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.cidr.known.project.marker&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.git.unshallow&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.readMode.enableVisualFormatting&quot;: &quot;true&quot;,
-    &quot;RunOnceActivity.typescript.service.memoryLimit.init&quot;: &quot;true&quot;,
-    &quot;Shell Script.Build with Batch Script.executor&quot;: &quot;Run&quot;,
-    &quot;Shell Script.Build with MSVC Tools.executor&quot;: &quot;Run&quot;,
-    &quot;cf.first.check.clang-format&quot;: &quot;false&quot;,
-    &quot;cidr.known.project.marker&quot;: &quot;true&quot;,
-    &quot;com.intellij.ml.llm.matterhorn.ej.ui.settings.DefaultModelSelectionForGA.v1&quot;: &quot;true&quot;,
-    &quot;git-widget-placeholder&quot;: &quot;main&quot;,
-    &quot;ignore.virus.scanning.warn.message&quot;: &quot;true&quot;,
-    &quot;junie.onboarding.icon.badge.shown&quot;: &quot;true&quot;,
-    &quot;last_opened_file_path&quot;: &quot;C:/Users/Brandon0/Desktop/dropmapreviewing/BetterAngle-GH&quot;,
-    &quot;node.js.detected.package.eslint&quot;: &quot;true&quot;,
-    &quot;node.js.detected.package.tslint&quot;: &quot;true&quot;,
-    &quot;node.js.selected.package.eslint&quot;: &quot;(autodetect)&quot;,
-    &quot;node.js.selected.package.tslint&quot;: &quot;(autodetect)&quot;,
-    &quot;nodejs_package_manager_path&quot;: &quot;npm&quot;,
-    &quot;org.rust.first.attach.projects&quot;: &quot;true&quot;,
-    &quot;settings.editor.selected.configurable&quot;: &quot;CPPToolchains&quot;,
-    &quot;to.speed.mode.migration.done&quot;: &quot;true&quot;,
-    &quot;vue.rearranger.settings.migration&quot;: &quot;true&quot;
+  <component name="PropertiesComponent"><![CDATA[{
+  "keyToString": {
+    "ASKED_MARK_IGNORED_FILES_AS_EXCLUDED": "true",
+    "C/C++ File.BetterAngle.cpp.executor": "Run",
+    "Custom Build Application.Main.executor": "Run",
+    "ModuleVcsDetector.initialDetectionPerformed": "true",
+    "RunOnceActivity.MCP Project settings loaded": "true",
+    "RunOnceActivity.RadMigrateCodeStyle": "true",
+    "RunOnceActivity.ShowReadmeOnStart": "true",
+    "RunOnceActivity.TerminalTabsStorage.copyFrom.TerminalArrangementManager.252": "true",
+    "RunOnceActivity.cidr.known.project.marker": "true",
+    "RunOnceActivity.git.unshallow": "true",
+    "RunOnceActivity.readMode.enableVisualFormatting": "true",
+    "RunOnceActivity.typescript.service.memoryLimit.init": "true",
+    "Shell Script.Build with Batch Script.executor": "Run",
+    "Shell Script.Build with MSVC Tools.executor": "Run",
+    "cf.first.check.clang-format": "false",
+    "cidr.known.project.marker": "true",
+    "com.intellij.ml.llm.matterhorn.ej.ui.settings.DefaultModelSelectionForGA.v1": "true",
+    "git-widget-placeholder": "Merging main",
+    "ignore.virus.scanning.warn.message": "true",
+    "junie.onboarding.icon.badge.shown": "true",
+    "last_opened_file_path": "C:/Users/Brandon0/Desktop/dropmapreviewing/BetterAngle-GH",
+    "node.js.detected.package.eslint": "true",
+    "node.js.detected.package.tslint": "true",
+    "node.js.selected.package.eslint": "(autodetect)",
+    "node.js.selected.package.tslint": "(autodetect)",
+    "nodejs_package_manager_path": "npm",
+    "org.rust.first.attach.projects": "true",
+    "settings.editor.selected.configurable": "CPPToolchains",
+    "to.speed.mode.migration.done": "true",
+    "vue.rearranger.settings.migration": "true"
   }
-}</component>
+}]]></component>
   <component name="RecapSpentCounter">
     <option name="endsOfQuotaMs" value="1776614410539" />
     <option name="spentUsd" value="0.081139795" />
@@ -132,7 +142,7 @@
       <workItem from="1775655893560" duration="899000" />
       <workItem from="1775656860147" duration="1022000" />
       <workItem from="1775660856789" duration="14832000" />
-      <workItem from="1775747915768" duration="142000" />
+      <workItem from="1775747915768" duration="813000" />
     </task>
     <servers />
   </component>
Index: CHANGELOG.md
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/CHANGELOG.md b/CHANGELOG.md
new file mode 100644
--- /dev/null	(date 1775748098230)
+++ b/CHANGELOG.md	(date 1775748098230)
@@ -0,0 +1,7 @@
+# Changelog
+
+## [4.9.37] - 2024-XX-XX
+### Fixed
+- Bug fixes and memory leak fixes.
+- Improved thread safety for shared state access.
+- Added proper cleanup for GDI and Direct2D resources.
\ No newline at end of file
Index: build.bat
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>@echo off\r\necho Building BetterAngle v4.9.15 Pro Edition...\r\n\r\n:: Check for MSVC\r\nwhere cl >nul 2>nul\r\nif %errorlevel% neq 0 (\r\n    echo [ERROR] MSVC (cl.exe) not found in PATH.\r\n    pause\r\n    exit /b 1\r\n)\r\n\r\n:: Create bin directory\r\nif not exist bin mkdir bin\r\n\r\n:: Shared Flags\r\nset FLAGS=/EHsc /O2 /DUNICODE /D_UNICODE /I include /I src\r\nset LIBS=user32.lib gdi32.lib gdiplus.lib dwmapi.lib winhttp.lib shell32.lib d2d1.lib dwrite.lib windowscodecs.lib /SUBSYSTEM:WINDOWS\r\n\r\n:: 1. Build Main Application\r\necho Building BetterAngle.exe (Main)...\r\ncl.exe %FLAGS% src/main_app/BetterAngle.cpp src/shared/*.cpp /Fe:bin/BetterAngle.exe /link %LIBS%\r\n\r\n:: 2. Build Calibration Tool\r\necho Building BetterAngleConfig.exe (Wizard)...\r\ncl.exe %FLAGS% src/config_tool/BetterAngleConfig.cpp src/shared/*.cpp /Fe:bin/BetterAngleConfig.exe /link %LIBS%\r\n\r\nif %errorlevel% equ 0 (\r\n    echo [SUCCESS] BetterAngle v4.9.15 Pro binaries created in bin/\r\n) else (\r\n    echo [ERROR] Build failed.\r\n)\r\n\r\npause\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/build.bat b/build.bat
--- a/build.bat	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/build.bat	(date 1775748098231)
@@ -1,33 +1,37 @@
 @echo off
-echo Building BetterAngle v4.9.15 Pro Edition...
+setlocal
+
+set "VER=unknown"
+if exist VERSION set /p VER=<VERSION
 
-:: Check for MSVC
+echo Building BetterAngle v%VER% Pro Edition...
+
 where cl >nul 2>nul
-if %errorlevel% neq 0 (
-    echo [ERROR] MSVC (cl.exe) not found in PATH.
-    pause
-    exit /b 1
-)
+if errorlevel 1 goto :msvc_missing
 
-:: Create bin directory
 if not exist bin mkdir bin
 
-:: Shared Flags
-set FLAGS=/EHsc /O2 /DUNICODE /D_UNICODE /I include /I src
-set LIBS=user32.lib gdi32.lib gdiplus.lib dwmapi.lib winhttp.lib shell32.lib d2d1.lib dwrite.lib windowscodecs.lib /SUBSYSTEM:WINDOWS
+set "FLAGS=/EHsc /O2 /DUNICODE /D_UNICODE /DAPP_VERSION=%VER% /Iinclude /Isrc"
+set "LIBS=user32.lib gdi32.lib gdiplus.lib dwmapi.lib winhttp.lib shell32.lib d2d1.lib dwrite.lib windowscodecs.lib /SUBSYSTEM:WINDOWS"
 
-:: 1. Build Main Application
 echo Building BetterAngle.exe (Main)...
 cl.exe %FLAGS% src/main_app/BetterAngle.cpp src/shared/*.cpp /Fe:bin/BetterAngle.exe /link %LIBS%
+if errorlevel 1 goto :build_failed
 
-:: 2. Build Calibration Tool
 echo Building BetterAngleConfig.exe (Wizard)...
 cl.exe %FLAGS% src/config_tool/BetterAngleConfig.cpp src/shared/*.cpp /Fe:bin/BetterAngleConfig.exe /link %LIBS%
+if errorlevel 1 goto :build_failed
+
+echo [SUCCESS] BetterAngle v%VER% binaries created in bin/
+goto :end
 
-if %errorlevel% equ 0 (
-    echo [SUCCESS] BetterAngle v4.9.15 Pro binaries created in bin/
-) else (
-    echo [ERROR] Build failed.
-)
+:msvc_missing
+echo [ERROR] MSVC (cl.exe) not found in PATH.
+goto :end
+
+:build_failed
+echo [ERROR] Build failed.
 
+:end
 pause
+endlocal
Index: src/shared/Overlay.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"shared/State.h\"\r\n#include \"shared/Logic.h\"\r\n#include <gdiplus.h>\r\n#include <iostream>\r\n#include <string>\r\n\r\nusing namespace Gdiplus;\r\n\r\nbool IsFortniteFocused();\r\n\r\nvoid DrawOverlay(HWND hwnd, double angle, const char* status, float detectionRatio, bool showCrosshair) {\r\n    PAINTSTRUCT ps;\r\n    HDC hdc = BeginPaint(hwnd, &ps);\r\n    \r\n    RECT rect;\r\n    GetClientRect(hwnd, &rect);\r\n    int sw = rect.right - rect.left;\r\n    int sh = rect.bottom - rect.top;\r\n    \r\n    HDC hdcMem = CreateCompatibleDC(hdc);\r\n    HBITMAP hbmMem = CreateCompatibleBitmap(hdc, sw, sh);\r\n    HGDIOBJ hOld = SelectObject(hdcMem, hbmMem);\r\n    \r\n    Graphics graphics(hdcMem);\r\n    graphics.SetSmoothingMode(Gdiplus::SmoothingModeAntiAlias);\r\n    graphics.SetTextRenderingHint(Gdiplus::TextRenderingHintClearTypeGridFit);\r\n    graphics.Clear(Gdiplus::Color(0, 0, 0, 0));\r\n\r\n    // Draw Background Snapshot (v4.9.15)\r\n    if (g_screenSnapshot && g_currentSelection != NONE) {\r\n        HDC hdcSnap = CreateCompatibleDC(hdcMem);\r\n        HGDIOBJ hOldSnap = SelectObject(hdcSnap, g_screenSnapshot);\r\n        BitBlt(hdcMem, 0, 0, sw, sh, hdcSnap, 0, 0, SRCCOPY);\r\n        SelectObject(hdcSnap, hOldSnap);\r\n        DeleteDC(hdcSnap);\r\n    }\r\n\r\n    // Cinematic Dimming & Two-Stage Selection\r\n    if (g_currentSelection != NONE) {\r\n        Gdiplus::SolidBrush dimBrush(Gdiplus::Color(100, 0, 0, 0)); // Light dim\r\n        graphics.FillRectangle(&dimBrush, 0, 0, sw, sh);\r\n        \r\n        Gdiplus::FontFamily fontFamily(L\"Segoe UI\");\r\n        Gdiplus::Font font(&fontFamily, 32, Gdiplus::FontStyleBold, Gdiplus::UnitPixel);\r\n        Gdiplus::SolidBrush whiteBrush(Gdiplus::Color(255, 255, 255, 255));\r\n        \r\n        if (g_currentSelection == SELECTING_ROI) {\r\n            graphics.DrawString(L\"STAGE 1: DRAG TO SELECT PROMPT AREA\", -1, &font, Gdiplus::PointF(50.0f, 50.0f), &whiteBrush);\r\n        } else if (g_currentSelection == SELECTING_COLOR) {\r\n            graphics.DrawString(L\"STAGE 2: CLICK TO PICK PRECISE COLOR\", -1, &font, Gdiplus::PointF(50.0f, 50.0f), &whiteBrush);\r\n            \r\n            // Follow-Mouse Magnifier Scope (+20x, +20y offset)\r\n            POINT mouse; GetCursorPos(&mouse);\r\n            int scopeSize = 200;\r\n            int offset = 20;\r\n            int scopeX = mouse.x + offset;\r\n            int scopeY = mouse.y + offset;\r\n\r\n            // Screen boundary check\r\n            if (scopeX + scopeSize > sw) scopeX = mouse.x - scopeSize - offset;\r\n            if (scopeY + scopeSize > sh) scopeY = mouse.y - scopeSize - offset;\r\n\r\n            GraphicsPath scopePath;\r\n            scopePath.AddEllipse(scopeX, scopeY, scopeSize, scopeSize);\r\n            graphics.SetClip(&scopePath);\r\n\r\n            // Fetch zoomed content from SNAPSHOT (No Dimming)\r\n            if (g_screenSnapshot) {\r\n                HDC hdcSnap = CreateCompatibleDC(hdcMem);\r\n                HGDIOBJ hOldSnap = SelectObject(hdcSnap, g_screenSnapshot);\r\n                StretchBlt(hdcMem, scopeX, scopeY, scopeSize, scopeSize, \r\n                           hdcSnap, mouse.x - 15, mouse.y - 15, 31, 31, SRCCOPY);\r\n                SelectObject(hdcSnap, hOldSnap);\r\n                DeleteDC(hdcSnap);\r\n            }\r\n\r\n            // Precision Red Dot Crosshair\r\n            SolidBrush redBrush(Color(255, 255, 0, 0));\r\n            int centerX = scopeX + scopeSize / 2;\r\n            int centerY = scopeY + scopeSize / 2;\r\n            graphics.FillEllipse(&redBrush, centerX - 1, centerY - 1, 3, 3);\r\n            \r\n            graphics.ResetClip();\r\n            graphics.DrawEllipse(&Pen(Color(255, 255, 255, 255), 3.0f), scopeX, scopeY, scopeSize, scopeSize);\r\n        }\r\n    }\r\n\r\n    // Dynamic Live ROI Box\r\n    if (g_showROIBox && g_selectionRect.right > g_selectionRect.left) {\r\n        Gdiplus::Color boxColor = g_isDiving ? Gdiplus::Color(255, 255, 0, 0) : Gdiplus::Color(255, 0, 255, 0);\r\n        if (g_currentSelection != NONE) boxColor = Gdiplus::Color(255, 0, 255, 0); \r\n        \r\n        Gdiplus::Pen roiPen(boxColor, 2.0f);\r\n        graphics.DrawRectangle(&roiPen, g_selectionRect.left, g_selectionRect.top, \r\n            g_selectionRect.right - g_selectionRect.left, g_selectionRect.bottom - g_selectionRect.top);\r\n    }\r\n\r\n    // Target Color Circle Feedback (BGR/RGB Sync Fix)\r\n    int circleX = 200; \r\n    int circleY = 100;\r\n    // User requested swaped B/R for sync: RGB(GetBValue, GetGValue, GetRValue)\r\n    Gdiplus::Color previewColor(255, GetBValue(g_targetColor), GetGValue(g_targetColor), GetRValue(g_targetColor));\r\n    Gdiplus::SolidBrush targetColorBrush(previewColor);\r\n    Gdiplus::Pen circleBorder(Gdiplus::Color(255, 200, 200, 200), 1.0f);\r\n\r\n    graphics.FillEllipse(&targetColorBrush, circleX, circleY, 15, 15);\r\n    graphics.DrawEllipse(&circleBorder, circleX, circleY, 15, 15);\r\n\r\n    // 2. Draw Precision Crosshair (F10)\r\n    if (showCrosshair) {\r\n        Gdiplus::Pen crossPen(Gdiplus::Color(255, 255, 0, 0), 1);\r\n        graphics.DrawLine(&crossPen, 0, sh / 2, sw, sh / 2);\r\n        graphics.DrawLine(&crossPen, sw / 2, 0, sw / 2, sh);\r\n    }\r\n\r\n    // 4. Draw Clean Glass HUD\r\n    int rx = 40, ry = 40, rw = 320, rh = 180, r = 20;\r\n    GraphicsPath path;\r\n    path.AddArc(rx, ry, r, r, 180, 90);\r\n    path.AddArc(rx + rw - r, ry, r, r, 270, 90);\r\n    path.AddArc(rx + rw - r, ry + rh - r, r, r, 0, 90);\r\n    path.AddArc(rx, ry + rh - r, r, r, 90, 90);\r\n    path.CloseFigure();\r\n\r\n    LinearGradientBrush brush(Point(rx, ry), Point(rx, ry + rh), \r\n                             Color(180, 15, 18, 22), Color(180, 5, 6, 8));\r\n    graphics.FillPath(&brush, &path);\r\n    Pen borderPen(Color(80, 255, 255, 255), 1);\r\n    graphics.DrawPath(&borderPen, &path);\r\n\r\n    // 5. Draw Angle Text\r\n    FontFamily fontFamily(L\"Segoe UI\");\r\n    Font font(&fontFamily, 64, FontStyleBold, UnitPixel);\r\n    SolidBrush textBrush(Color(255, 0, 255, 127)); \r\n\r\n    std::wstring angleStr = std::to_wstring(angle);\r\n    angleStr = angleStr.substr(0, angleStr.find(L'.') + 2) + L\"\\u00B0\"; \r\n    graphics.DrawString(angleStr.c_str(), -1, &font, PointF(rx + 30, ry + 45), &textBrush);\r\n\r\n    // 6. Match Percentage & Indicator\r\n    int matchPct = (int)(detectionRatio * 100);\r\n    std::wstring matchStr = L\"Match: \" + std::to_wstring(matchPct) + L\"%\";\r\n    Font subFont(&fontFamily, 14, FontStyleBold, UnitPixel);\r\n    SolidBrush greyBrush(Color(255, 160, 170, 180));\r\n    graphics.DrawString(matchStr.c_str(), -1, &subFont, PointF(rx + 30, ry + 115), &greyBrush);\r\n\r\n    Color indicatorColor = Color(255, 0, 255, 127); \r\n    if (detectionRatio > 0.05f) indicatorColor = Color(255, 0, 255, 255); \r\n    graphics.FillEllipse(&SolidBrush(indicatorColor), rx + 205, ry + 75, 14, 14); \r\n\r\n    // 7. Mini Labels\r\n    graphics.DrawString(L\"CURRENT ANGLE (LIVE)\", -1, &subFont, PointF(rx + 30, ry + 25), &greyBrush);\r\n\r\n#ifndef APP_VERSION_WSTR\r\n#define APP_VERSION_WSTR L\"4.9.36\"\r\n#endif\r\n\r\n    Font miniFont(&fontFamily, 10, FontStyleRegular, UnitPixel);\r\n    graphics.DrawString(L\"BetterAngle Pro v\" APP_VERSION_WSTR L\" | Auto-Installer Ready\", -1, &miniFont, PointF(rx + 30, ry + 150), &greyBrush);\r\n\r\n    // 8. Debug Menu (Ctrl + 9)\r\n    if (g_debugMode) {\r\n        int dx = 40, dy = 250, dw = 320, dh = 180;\r\n        GraphicsPath dbgPath;\r\n        dbgPath.AddRectangle(Rect(dx, dy, dw, dh));\r\n        graphics.FillPath(&SolidBrush(Color(200, 10, 10, 15)), &dbgPath);\r\n        graphics.DrawPath(&Pen(Color(150, 255, 255, 255), 1), &dbgPath);\r\n\r\n        Font dbgTitleFont(&fontFamily, 14, FontStyleBold, UnitPixel);\r\n        Font dbgFont(&fontFamily, 11, FontStyleRegular, UnitPixel);\r\n        SolidBrush whiteBrush(Color(255, 255, 255, 255));\r\n        SolidBrush yellowBrush(Color(255, 255, 255, 0));\r\n\r\n        graphics.DrawString(L\"DEBUG DASHBOARD\", -1, &dbgTitleFont, PointF(dx + 15, dy + 10), &yellowBrush);\r\n\r\n        std::wstring rawAngle = L\"Raw Angle: \" + std::to_wstring(angle);\r\n        std::wstring focusStr = L\"Fortnite Focused: \";\r\n        focusStr += IsFortniteFocused() ? L\"YES\" : L\"NO\";\r\n        \r\n        std::wstring scaleStr = L\"Active Scale: \" + std::to_wstring(g_logic.GetScale());\r\n        std::wstring detStr = L\"Raw Detection: \" + std::to_wstring(detectionRatio);\r\n        \r\n        std::wstring cursorStr = L\"Cursor Visible: \";\r\n        cursorStr += g_isCursorVisible ? L\"YES\" : L\"NO\";\r\n        \r\n        std::wstring selectionStr = L\"Selection State: \";\r\n        selectionStr += (g_currentSelection == NONE) ? L\"NONE\" : ((g_currentSelection == SELECTING_ROI) ? L\"ROI\" : L\"COLOR\");\r\n\r\n        graphics.DrawString(rawAngle.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 40), &whiteBrush);\r\n        graphics.DrawString(focusStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 60), &whiteBrush);\r\n        graphics.DrawString(scaleStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 80), &whiteBrush);\r\n        graphics.DrawString(detStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 100), &whiteBrush);\r\n        graphics.DrawString(cursorStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 120), &whiteBrush);\r\n        graphics.DrawString(selectionStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 140), &whiteBrush);\r\n        graphics.DrawString(L\"Mode: SECRET DEBUG ACTIVE\", -1, &dbgFont, PointF(dx + 15, dy + 160), &yellowBrush);\r\n    }\r\n\r\n    BitBlt(hdc, 0, 0, sw, sh, hdcMem, 0, 0, SRCCOPY);\r\n    SelectObject(hdcMem, hOld);\r\n    DeleteObject(hbmMem);\r\n    DeleteDC(hdcMem);\r\n    EndPaint(hwnd, &ps);\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/shared/Overlay.cpp b/src/shared/Overlay.cpp
--- a/src/shared/Overlay.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/shared/Overlay.cpp	(date 1775748098235)
@@ -1,203 +1,248 @@
-#include "shared/State.h"
 #include "shared/Logic.h"
+#include "shared/State.h"
 #include <gdiplus.h>
 #include <iostream>
+#include <mutex>
 #include <string>
 
 using namespace Gdiplus;
 
 bool IsFortniteFocused();
 
-void DrawOverlay(HWND hwnd, double angle, const char* status, float detectionRatio, bool showCrosshair) {
-    PAINTSTRUCT ps;
-    HDC hdc = BeginPaint(hwnd, &ps);
-    
-    RECT rect;
-    GetClientRect(hwnd, &rect);
-    int sw = rect.right - rect.left;
-    int sh = rect.bottom - rect.top;
-    
-    HDC hdcMem = CreateCompatibleDC(hdc);
-    HBITMAP hbmMem = CreateCompatibleBitmap(hdc, sw, sh);
-    HGDIOBJ hOld = SelectObject(hdcMem, hbmMem);
-    
-    Graphics graphics(hdcMem);
-    graphics.SetSmoothingMode(Gdiplus::SmoothingModeAntiAlias);
-    graphics.SetTextRenderingHint(Gdiplus::TextRenderingHintClearTypeGridFit);
-    graphics.Clear(Gdiplus::Color(0, 0, 0, 0));
+void DrawOverlay(HWND hwnd, double angle, const char *status,
+                 float detectionRatio, bool showCrosshair) {
+  PAINTSTRUCT ps;
+  HDC hdc = BeginPaint(hwnd, &ps);
+
+  RECT rect;
+  GetClientRect(hwnd, &rect);
+  int sw = rect.right - rect.left;
+  int sh = rect.bottom - rect.top;
+
+  HDC hdcMem = CreateCompatibleDC(hdc);
+  HBITMAP hbmMem = CreateCompatibleBitmap(hdc, sw, sh);
+  HGDIOBJ hOld = SelectObject(hdcMem, hbmMem);
+
+  Graphics graphics(hdcMem);
+  graphics.SetSmoothingMode(Gdiplus::SmoothingModeAntiAlias);
+  graphics.SetTextRenderingHint(Gdiplus::TextRenderingHintClearTypeGridFit);
+  graphics.Clear(Gdiplus::Color(0, 0, 0, 0));
 
-    // Draw Background Snapshot (v4.9.15)
+  // Draw Background Snapshot (v4.9.15)
+  {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
     if (g_screenSnapshot && g_currentSelection != NONE) {
-        HDC hdcSnap = CreateCompatibleDC(hdcMem);
-        HGDIOBJ hOldSnap = SelectObject(hdcSnap, g_screenSnapshot);
-        BitBlt(hdcMem, 0, 0, sw, sh, hdcSnap, 0, 0, SRCCOPY);
-        SelectObject(hdcSnap, hOldSnap);
-        DeleteDC(hdcSnap);
+      HDC hdcSnap = CreateCompatibleDC(hdcMem);
+      HGDIOBJ hOldSnap = SelectObject(hdcSnap, g_screenSnapshot);
+      BitBlt(hdcMem, 0, 0, sw, sh, hdcSnap, 0, 0, SRCCOPY);
+      SelectObject(hdcSnap, hOldSnap);
+      DeleteDC(hdcSnap);
     }
+  }
 
-    // Cinematic Dimming & Two-Stage Selection
+  // Cinematic Dimming & Two-Stage Selection
+  {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
     if (g_currentSelection != NONE) {
-        Gdiplus::SolidBrush dimBrush(Gdiplus::Color(100, 0, 0, 0)); // Light dim
-        graphics.FillRectangle(&dimBrush, 0, 0, sw, sh);
-        
-        Gdiplus::FontFamily fontFamily(L"Segoe UI");
-        Gdiplus::Font font(&fontFamily, 32, Gdiplus::FontStyleBold, Gdiplus::UnitPixel);
-        Gdiplus::SolidBrush whiteBrush(Gdiplus::Color(255, 255, 255, 255));
-        
-        if (g_currentSelection == SELECTING_ROI) {
-            graphics.DrawString(L"STAGE 1: DRAG TO SELECT PROMPT AREA", -1, &font, Gdiplus::PointF(50.0f, 50.0f), &whiteBrush);
-        } else if (g_currentSelection == SELECTING_COLOR) {
-            graphics.DrawString(L"STAGE 2: CLICK TO PICK PRECISE COLOR", -1, &font, Gdiplus::PointF(50.0f, 50.0f), &whiteBrush);
-            
-            // Follow-Mouse Magnifier Scope (+20x, +20y offset)
-            POINT mouse; GetCursorPos(&mouse);
-            int scopeSize = 200;
-            int offset = 20;
-            int scopeX = mouse.x + offset;
-            int scopeY = mouse.y + offset;
+      Gdiplus::SolidBrush dimBrush(Gdiplus::Color(100, 0, 0, 0)); // Light dim
+      graphics.FillRectangle(&dimBrush, 0, 0, sw, sh);
+
+      Gdiplus::FontFamily fontFamily(L"Segoe UI");
+      Gdiplus::Font font(&fontFamily, 32, Gdiplus::FontStyleBold,
+                         Gdiplus::UnitPixel);
+      Gdiplus::SolidBrush whiteBrush(Gdiplus::Color(255, 255, 255, 255));
+
+      if (g_currentSelection == SELECTING_ROI) {
+        graphics.DrawString(L"STAGE 1: DRAG TO SELECT PROMPT AREA", -1, &font,
+                            Gdiplus::PointF(50.0f, 50.0f), &whiteBrush);
+      } else if (g_currentSelection == SELECTING_COLOR) {
+        graphics.DrawString(L"STAGE 2: CLICK TO PICK PRECISE COLOR", -1, &font,
+                            Gdiplus::PointF(50.0f, 50.0f), &whiteBrush);
+
+        // Follow-Mouse Magnifier Scope (+20x, +20y offset)
+        POINT mouse;
+        GetCursorPos(&mouse);
+        int scopeSize = 200;
+        int offset = 20;
+        int scopeX = mouse.x + offset;
+        int scopeY = mouse.y + offset;
 
-            // Screen boundary check
-            if (scopeX + scopeSize > sw) scopeX = mouse.x - scopeSize - offset;
-            if (scopeY + scopeSize > sh) scopeY = mouse.y - scopeSize - offset;
+        // Screen boundary check
+        if (scopeX + scopeSize > sw)
+          scopeX = mouse.x - scopeSize - offset;
+        if (scopeY + scopeSize > sh)
+          scopeY = mouse.y - scopeSize - offset;
 
-            GraphicsPath scopePath;
-            scopePath.AddEllipse(scopeX, scopeY, scopeSize, scopeSize);
-            graphics.SetClip(&scopePath);
+        GraphicsPath scopePath;
+        scopePath.AddEllipse(scopeX, scopeY, scopeSize, scopeSize);
+        graphics.SetClip(&scopePath);
 
-            // Fetch zoomed content from SNAPSHOT (No Dimming)
-            if (g_screenSnapshot) {
-                HDC hdcSnap = CreateCompatibleDC(hdcMem);
-                HGDIOBJ hOldSnap = SelectObject(hdcSnap, g_screenSnapshot);
-                StretchBlt(hdcMem, scopeX, scopeY, scopeSize, scopeSize, 
-                           hdcSnap, mouse.x - 15, mouse.y - 15, 31, 31, SRCCOPY);
-                SelectObject(hdcSnap, hOldSnap);
-                DeleteDC(hdcSnap);
-            }
+        // Fetch zoomed content from SNAPSHOT (No Dimming)
+        if (g_screenSnapshot) {
+          HDC hdcSnap = CreateCompatibleDC(hdcMem);
+          HGDIOBJ hOldSnap = SelectObject(hdcSnap, g_screenSnapshot);
+          StretchBlt(hdcMem, scopeX, scopeY, scopeSize, scopeSize, hdcSnap,
+                     mouse.x - 15, mouse.y - 15, 31, 31, SRCCOPY);
+          SelectObject(hdcSnap, hOldSnap);
+          DeleteDC(hdcSnap);
+        }
 
-            // Precision Red Dot Crosshair
-            SolidBrush redBrush(Color(255, 255, 0, 0));
-            int centerX = scopeX + scopeSize / 2;
-            int centerY = scopeY + scopeSize / 2;
-            graphics.FillEllipse(&redBrush, centerX - 1, centerY - 1, 3, 3);
-            
-            graphics.ResetClip();
-            graphics.DrawEllipse(&Pen(Color(255, 255, 255, 255), 3.0f), scopeX, scopeY, scopeSize, scopeSize);
-        }
+        // Precision Red Dot Crosshair
+        SolidBrush redBrush(Color(255, 255, 0, 0));
+        int centerX = scopeX + scopeSize / 2;
+        int centerY = scopeY + scopeSize / 2;
+        graphics.FillEllipse(&redBrush, centerX - 1, centerY - 1, 3, 3);
+
+        graphics.ResetClip();
+        graphics.DrawEllipse(&Pen(Color(255, 255, 255, 255), 3.0f), scopeX,
+                             scopeY, scopeSize, scopeSize);
+      }
     }
+  }
 
-    // Dynamic Live ROI Box
+  // Dynamic Live ROI Box
+  {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
     if (g_showROIBox && g_selectionRect.right > g_selectionRect.left) {
-        Gdiplus::Color boxColor = g_isDiving ? Gdiplus::Color(255, 255, 0, 0) : Gdiplus::Color(255, 0, 255, 0);
-        if (g_currentSelection != NONE) boxColor = Gdiplus::Color(255, 0, 255, 0); 
-        
-        Gdiplus::Pen roiPen(boxColor, 2.0f);
-        graphics.DrawRectangle(&roiPen, g_selectionRect.left, g_selectionRect.top, 
-            g_selectionRect.right - g_selectionRect.left, g_selectionRect.bottom - g_selectionRect.top);
+      Gdiplus::Color boxColor = g_isDiving ? Gdiplus::Color(255, 255, 0, 0)
+                                           : Gdiplus::Color(255, 0, 255, 0);
+      if (g_currentSelection != NONE)
+        boxColor = Gdiplus::Color(255, 0, 255, 0);
+
+      Gdiplus::Pen roiPen(boxColor, 2.0f);
+      graphics.DrawRectangle(&roiPen, g_selectionRect.left, g_selectionRect.top,
+                             g_selectionRect.right - g_selectionRect.left,
+                             g_selectionRect.bottom - g_selectionRect.top);
     }
+  }
 
-    // Target Color Circle Feedback (BGR/RGB Sync Fix)
-    int circleX = 200; 
-    int circleY = 100;
-    // User requested swaped B/R for sync: RGB(GetBValue, GetGValue, GetRValue)
-    Gdiplus::Color previewColor(255, GetBValue(g_targetColor), GetGValue(g_targetColor), GetRValue(g_targetColor));
-    Gdiplus::SolidBrush targetColorBrush(previewColor);
-    Gdiplus::Pen circleBorder(Gdiplus::Color(255, 200, 200, 200), 1.0f);
+  // Target Color Circle Feedback (BGR/RGB Sync Fix)
+  int circleX = 200;
+  int circleY = 100;
+  // User requested swaped B/R for sync: RGB(GetBValue, GetGValue, GetRValue)
+  Gdiplus::Color previewColor(255, GetBValue(g_targetColor),
+                              GetGValue(g_targetColor),
+                              GetRValue(g_targetColor));
+  Gdiplus::SolidBrush targetColorBrush(previewColor);
+  Gdiplus::Pen circleBorder(Gdiplus::Color(255, 200, 200, 200), 1.0f);
 
-    graphics.FillEllipse(&targetColorBrush, circleX, circleY, 15, 15);
-    graphics.DrawEllipse(&circleBorder, circleX, circleY, 15, 15);
+  graphics.FillEllipse(&targetColorBrush, circleX, circleY, 15, 15);
+  graphics.DrawEllipse(&circleBorder, circleX, circleY, 15, 15);
 
-    // 2. Draw Precision Crosshair (F10)
-    if (showCrosshair) {
-        Gdiplus::Pen crossPen(Gdiplus::Color(255, 255, 0, 0), 1);
-        graphics.DrawLine(&crossPen, 0, sh / 2, sw, sh / 2);
-        graphics.DrawLine(&crossPen, sw / 2, 0, sw / 2, sh);
-    }
+  // 2. Draw Precision Crosshair (F10)
+  if (showCrosshair) {
+    Gdiplus::Pen crossPen(Gdiplus::Color(255, 255, 0, 0), 1);
+    graphics.DrawLine(&crossPen, 0, sh / 2, sw, sh / 2);
+    graphics.DrawLine(&crossPen, sw / 2, 0, sw / 2, sh);
+  }
 
-    // 4. Draw Clean Glass HUD
-    int rx = 40, ry = 40, rw = 320, rh = 180, r = 20;
-    GraphicsPath path;
-    path.AddArc(rx, ry, r, r, 180, 90);
-    path.AddArc(rx + rw - r, ry, r, r, 270, 90);
-    path.AddArc(rx + rw - r, ry + rh - r, r, r, 0, 90);
-    path.AddArc(rx, ry + rh - r, r, r, 90, 90);
-    path.CloseFigure();
+  // 4. Draw Clean Glass HUD
+  int rx = 40, ry = 40, rw = 320, rh = 180, r = 20;
+  GraphicsPath path;
+  path.AddArc(rx, ry, r, r, 180, 90);
+  path.AddArc(rx + rw - r, ry, r, r, 270, 90);
+  path.AddArc(rx + rw - r, ry + rh - r, r, r, 0, 90);
+  path.AddArc(rx, ry + rh - r, r, r, 90, 90);
+  path.CloseFigure();
 
-    LinearGradientBrush brush(Point(rx, ry), Point(rx, ry + rh), 
-                             Color(180, 15, 18, 22), Color(180, 5, 6, 8));
-    graphics.FillPath(&brush, &path);
-    Pen borderPen(Color(80, 255, 255, 255), 1);
-    graphics.DrawPath(&borderPen, &path);
+  LinearGradientBrush brush(Point(rx, ry), Point(rx, ry + rh),
+                            Color(180, 15, 18, 22), Color(180, 5, 6, 8));
+  graphics.FillPath(&brush, &path);
+  Pen borderPen(Color(80, 255, 255, 255), 1);
+  graphics.DrawPath(&borderPen, &path);
 
-    // 5. Draw Angle Text
-    FontFamily fontFamily(L"Segoe UI");
-    Font font(&fontFamily, 64, FontStyleBold, UnitPixel);
-    SolidBrush textBrush(Color(255, 0, 255, 127)); 
+  // 5. Draw Angle Text
+  FontFamily fontFamily(L"Segoe UI");
+  Font font(&fontFamily, 64, FontStyleBold, UnitPixel);
+  SolidBrush textBrush(Color(255, 0, 255, 127));
 
-    std::wstring angleStr = std::to_wstring(angle);
-    angleStr = angleStr.substr(0, angleStr.find(L'.') + 2) + L"\u00B0"; 
-    graphics.DrawString(angleStr.c_str(), -1, &font, PointF(rx + 30, ry + 45), &textBrush);
+  std::wstring angleStr = std::to_wstring(angle);
+  angleStr = angleStr.substr(0, angleStr.find(L'.') + 2) + L"\u00B0";
+  graphics.DrawString(angleStr.c_str(), -1, &font, PointF(rx + 30, ry + 45),
+                      &textBrush);
 
-    // 6. Match Percentage & Indicator
-    int matchPct = (int)(detectionRatio * 100);
-    std::wstring matchStr = L"Match: " + std::to_wstring(matchPct) + L"%";
-    Font subFont(&fontFamily, 14, FontStyleBold, UnitPixel);
-    SolidBrush greyBrush(Color(255, 160, 170, 180));
-    graphics.DrawString(matchStr.c_str(), -1, &subFont, PointF(rx + 30, ry + 115), &greyBrush);
+  // 6. Match Percentage & Indicator
+  int matchPct = (int)(detectionRatio * 100);
+  std::wstring matchStr = L"Match: " + std::to_wstring(matchPct) + L"%";
+  Font subFont(&fontFamily, 14, FontStyleBold, UnitPixel);
+  SolidBrush greyBrush(Color(255, 160, 170, 180));
+  graphics.DrawString(matchStr.c_str(), -1, &subFont, PointF(rx + 30, ry + 115),
+                      &greyBrush);
 
-    Color indicatorColor = Color(255, 0, 255, 127); 
-    if (detectionRatio > 0.05f) indicatorColor = Color(255, 0, 255, 255); 
-    graphics.FillEllipse(&SolidBrush(indicatorColor), rx + 205, ry + 75, 14, 14); 
+  Color indicatorColor = Color(255, 0, 255, 127);
+  if (detectionRatio > 0.05f)
+    indicatorColor = Color(255, 0, 255, 255);
+  graphics.FillEllipse(&SolidBrush(indicatorColor), rx + 205, ry + 75, 14, 14);
 
-    // 7. Mini Labels
-    graphics.DrawString(L"CURRENT ANGLE (LIVE)", -1, &subFont, PointF(rx + 30, ry + 25), &greyBrush);
+  // 7. Mini Labels
+  graphics.DrawString(L"CURRENT ANGLE (LIVE)", -1, &subFont,
+                      PointF(rx + 30, ry + 25), &greyBrush);
 
 #ifndef APP_VERSION_WSTR
 #define APP_VERSION_WSTR L"4.9.36"
 #endif
 
-    Font miniFont(&fontFamily, 10, FontStyleRegular, UnitPixel);
-    graphics.DrawString(L"BetterAngle Pro v" APP_VERSION_WSTR L" | Auto-Installer Ready", -1, &miniFont, PointF(rx + 30, ry + 150), &greyBrush);
+  Font miniFont(&fontFamily, 10, FontStyleRegular, UnitPixel);
+  graphics.DrawString(
+      L"BetterAngle Pro v" APP_VERSION_WSTR L" | Auto-Installer Ready", -1,
+      &miniFont, PointF(rx + 30, ry + 150), &greyBrush);
 
-    // 8. Debug Menu (Ctrl + 9)
+  // 8. Debug Menu (Ctrl + 9)
+  {
+    std::lock_guard<std::mutex> lock(g_stateMutex);
     if (g_debugMode) {
-        int dx = 40, dy = 250, dw = 320, dh = 180;
-        GraphicsPath dbgPath;
-        dbgPath.AddRectangle(Rect(dx, dy, dw, dh));
-        graphics.FillPath(&SolidBrush(Color(200, 10, 10, 15)), &dbgPath);
-        graphics.DrawPath(&Pen(Color(150, 255, 255, 255), 1), &dbgPath);
+      int dx = 40, dy = 250, dw = 320, dh = 180;
+      GraphicsPath dbgPath;
+      dbgPath.AddRectangle(Rect(dx, dy, dw, dh));
+      graphics.FillPath(&SolidBrush(Color(200, 10, 10, 15)), &dbgPath);
+      graphics.DrawPath(&Pen(Color(150, 255, 255, 255), 1), &dbgPath);
 
-        Font dbgTitleFont(&fontFamily, 14, FontStyleBold, UnitPixel);
-        Font dbgFont(&fontFamily, 11, FontStyleRegular, UnitPixel);
-        SolidBrush whiteBrush(Color(255, 255, 255, 255));
-        SolidBrush yellowBrush(Color(255, 255, 255, 0));
+      Font dbgTitleFont(&fontFamily, 14, FontStyleBold, UnitPixel);
+      Font dbgFont(&fontFamily, 11, FontStyleRegular, UnitPixel);
+      SolidBrush whiteBrush(Color(255, 255, 255, 255));
+      SolidBrush yellowBrush(Color(255, 255, 255, 0));
 
-        graphics.DrawString(L"DEBUG DASHBOARD", -1, &dbgTitleFont, PointF(dx + 15, dy + 10), &yellowBrush);
+      graphics.DrawString(L"DEBUG DASHBOARD", -1, &dbgTitleFont,
+                          PointF(dx + 15, dy + 10), &yellowBrush);
 
-        std::wstring rawAngle = L"Raw Angle: " + std::to_wstring(angle);
-        std::wstring focusStr = L"Fortnite Focused: ";
-        focusStr += IsFortniteFocused() ? L"YES" : L"NO";
-        
-        std::wstring scaleStr = L"Active Scale: " + std::to_wstring(g_logic.GetScale());
-        std::wstring detStr = L"Raw Detection: " + std::to_wstring(detectionRatio);
-        
-        std::wstring cursorStr = L"Cursor Visible: ";
-        cursorStr += g_isCursorVisible ? L"YES" : L"NO";
-        
-        std::wstring selectionStr = L"Selection State: ";
-        selectionStr += (g_currentSelection == NONE) ? L"NONE" : ((g_currentSelection == SELECTING_ROI) ? L"ROI" : L"COLOR");
+      std::wstring rawAngle = L"Raw Angle: " + std::to_wstring(angle);
+      std::wstring focusStr = L"Fortnite Focused: ";
+      focusStr += IsFortniteFocused() ? L"YES" : L"NO";
+
+      std::wstring scaleStr =
+          L"Active Scale: " + std::to_wstring(g_logic.GetScale());
+      std::wstring detStr =
+          L"Raw Detection: " + std::to_wstring(detectionRatio);
+
+      std::wstring cursorStr = L"Cursor Visible: ";
+      cursorStr += g_isCursorVisible ? L"YES" : L"NO";
+
+      std::wstring selectionStr = L"Selection State: ";
+      selectionStr +=
+          (g_currentSelection == NONE)
+              ? L"NONE"
+              : ((g_currentSelection == SELECTING_ROI) ? L"ROI" : L"COLOR");
 
-        graphics.DrawString(rawAngle.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 40), &whiteBrush);
-        graphics.DrawString(focusStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 60), &whiteBrush);
-        graphics.DrawString(scaleStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 80), &whiteBrush);
-        graphics.DrawString(detStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 100), &whiteBrush);
-        graphics.DrawString(cursorStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 120), &whiteBrush);
-        graphics.DrawString(selectionStr.c_str(), -1, &dbgFont, PointF(dx + 15, dy + 140), &whiteBrush);
-        graphics.DrawString(L"Mode: SECRET DEBUG ACTIVE", -1, &dbgFont, PointF(dx + 15, dy + 160), &yellowBrush);
+      graphics.DrawString(rawAngle.c_str(), -1, &dbgFont,
+                          PointF(dx + 15, dy + 40), &whiteBrush);
+      graphics.DrawString(focusStr.c_str(), -1, &dbgFont,
+                          PointF(dx + 15, dy + 60), &whiteBrush);
+      graphics.DrawString(scaleStr.c_str(), -1, &dbgFont,
+                          PointF(dx + 15, dy + 80), &whiteBrush);
+      graphics.DrawString(detStr.c_str(), -1, &dbgFont,
+                          PointF(dx + 15, dy + 100), &whiteBrush);
+      graphics.DrawString(cursorStr.c_str(), -1, &dbgFont,
+                          PointF(dx + 15, dy + 120), &whiteBrush);
+      graphics.DrawString(selectionStr.c_str(), -1, &dbgFont,
+                          PointF(dx + 15, dy + 140), &whiteBrush);
+      graphics.DrawString(L"Mode: SECRET DEBUG ACTIVE", -1, &dbgFont,
+                          PointF(dx + 15, dy + 160), &yellowBrush);
     }
+  }
 
-    BitBlt(hdc, 0, 0, sw, sh, hdcMem, 0, 0, SRCCOPY);
-    SelectObject(hdcMem, hOld);
-    DeleteObject(hbmMem);
-    DeleteDC(hdcMem);
-    EndPaint(hwnd, &ps);
+  BitBlt(hdc, 0, 0, sw, sh, hdcMem, 0, 0, SRCCOPY);
+  SelectObject(hdcMem, hOld);
+  DeleteObject(hbmMem);
+  DeleteDC(hdcMem);
+  EndPaint(hwnd, &ps);
 }
Index: src/shared/Detector.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"shared/Detector.h\"\r\n#include <iostream>\r\n#include <cmath>\r\n#include <vector>\r\n\r\nFovDetector::FovDetector() \r\n    : m_hdcScreen(NULL), m_hdcMem(NULL), m_hbm(NULL), m_hOld(NULL), m_curW(0), m_curH(0) {\r\n    m_hdcScreen = GetDC(NULL);\r\n}\r\n\r\nFovDetector::~FovDetector() {\r\n    if (m_hdcMem) {\r\n        SelectObject(m_hdcMem, m_hOld);\r\n        DeleteDC(m_hdcMem);\r\n    }\r\n    if (m_hbm) DeleteObject(m_hbm);\r\n    ReleaseDC(NULL, m_hdcScreen);\r\n}\r\n\r\nvoid FovDetector::EnsureResources(int w, int h) {\r\n    if (w == m_curW && h == m_curH && m_hdcMem) return;\r\n\r\n    if (m_hdcMem) {\r\n        SelectObject(m_hdcMem, m_hOld);\r\n        DeleteDC(m_hdcMem);\r\n        DeleteObject(m_hbm);\r\n    }\r\n\r\n    m_hdcMem = CreateCompatibleDC(m_hdcScreen);\r\n    m_hbm = CreateCompatibleBitmap(m_hdcScreen, w, h);\r\n    m_hOld = SelectObject(m_hdcMem, m_hbm);\r\n    m_curW = w;\r\n    m_curH = h;\r\n}\r\n\r\nfloat FovDetector::Scan(const RoiConfig& cfg) {\r\n    if (cfg.w <= 0 || cfg.h <= 0) return 0.0f;\r\n    \r\n    EnsureResources(cfg.w, cfg.h);\r\n    BitBlt(m_hdcMem, 0, 0, cfg.w, cfg.h, m_hdcScreen, cfg.x, cfg.y, SRCCOPY);\r\n\r\n    BITMAPINFO bmi = { 0 };\r\n    bmi.bmiHeader.biSize = sizeof(BITMAPINFOHEADER);\r\n    bmi.bmiHeader.biWidth = cfg.w;\r\n    bmi.bmiHeader.biHeight = -cfg.h; // Top-down\r\n    bmi.bmiHeader.biPlanes = 1;\r\n    bmi.bmiHeader.biBitCount = 32;\r\n    bmi.bmiHeader.biCompression = BI_RGB;\r\n\r\n    std::vector<DWORD> pixels(cfg.w * cfg.h);\r\n    if (GetDIBits(m_hdcMem, m_hbm, 0, cfg.h, pixels.data(), &bmi, DIB_RGB_COLORS) == 0) {\r\n        return 0.0f;\r\n    }\r\n\r\n    int match = 0;\r\n    BYTE tr = GetRValue(cfg.target);\r\n    BYTE tg = GetGValue(cfg.target);\r\n    BYTE tb = GetBValue(cfg.target);\r\n\r\n    for (DWORD p : pixels) {\r\n        BYTE b = p & 0xFF;\r\n        BYTE g = (p >> 8) & 0xFF;\r\n        BYTE r = (p >> 16) & 0xFF;\r\n\r\n        if (abs(r - tr) <= cfg.tolerance && \r\n            abs(g - tg) <= cfg.tolerance && \r\n            abs(b - tb) <= cfg.tolerance) {\r\n            match++;\r\n        }\r\n    }\r\n\r\n    return (float)match / (float)(cfg.w * cfg.h);\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/shared/Detector.cpp b/src/shared/Detector.cpp
--- a/src/shared/Detector.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/shared/Detector.cpp	(date 1775748098234)
@@ -1,73 +1,101 @@
 #include "shared/Detector.h"
-#include <iostream>
 #include <cmath>
+#include <iostream>
 #include <vector>
 
-FovDetector::FovDetector() 
-    : m_hdcScreen(NULL), m_hdcMem(NULL), m_hbm(NULL), m_hOld(NULL), m_curW(0), m_curH(0) {
-    m_hdcScreen = GetDC(NULL);
+FovDetector::FovDetector()
+    : m_hdcScreen(NULL), m_hdcMem(NULL), m_hbm(NULL), m_hOld(NULL), m_curW(0),
+      m_curH(0) {
+  m_hdcScreen = GetDC(NULL);
+  if (!m_hdcScreen) {
+    throw std::runtime_error("Failed to get screen DC");
+  }
 }
 
 FovDetector::~FovDetector() {
-    if (m_hdcMem) {
-        SelectObject(m_hdcMem, m_hOld);
-        DeleteDC(m_hdcMem);
-    }
-    if (m_hbm) DeleteObject(m_hbm);
+  if (m_hdcMem) {
+    if (m_hOld) {
+      SelectObject(m_hdcMem, m_hOld);
+    }
+    DeleteDC(m_hdcMem);
+  }
+  if (m_hbm) {
+    DeleteObject(m_hbm);
+  }
+  if (m_hdcScreen) {
     ReleaseDC(NULL, m_hdcScreen);
+  }
 }
 
 void FovDetector::EnsureResources(int w, int h) {
-    if (w == m_curW && h == m_curH && m_hdcMem) return;
+  if (w == m_curW && h == m_curH && m_hdcMem)
+    return;
 
-    if (m_hdcMem) {
-        SelectObject(m_hdcMem, m_hOld);
-        DeleteDC(m_hdcMem);
-        DeleteObject(m_hbm);
-    }
+  if (m_hdcMem) {
+    if (m_hOld) {
+      SelectObject(m_hdcMem, m_hOld);
+    }
+    DeleteDC(m_hdcMem);
+  }
+  if (m_hbm) {
+    DeleteObject(m_hbm);
+  }
 
-    m_hdcMem = CreateCompatibleDC(m_hdcScreen);
-    m_hbm = CreateCompatibleBitmap(m_hdcScreen, w, h);
-    m_hOld = SelectObject(m_hdcMem, m_hbm);
-    m_curW = w;
-    m_curH = h;
+  m_hdcMem = CreateCompatibleDC(m_hdcScreen);
+  if (!m_hdcMem)
+    return;
+
+  m_hbm = CreateCompatibleBitmap(m_hdcScreen, w, h);
+  if (!m_hbm)
+    return;
+
+  m_hOld = SelectObject(m_hdcMem, m_hbm);
+  m_curW = w;
+  m_curH = h;
 }
 
-float FovDetector::Scan(const RoiConfig& cfg) {
-    if (cfg.w <= 0 || cfg.h <= 0) return 0.0f;
-    
-    EnsureResources(cfg.w, cfg.h);
-    BitBlt(m_hdcMem, 0, 0, cfg.w, cfg.h, m_hdcScreen, cfg.x, cfg.y, SRCCOPY);
+float FovDetector::Scan(const RoiConfig &cfg) {
+  if (cfg.w <= 0 || cfg.h <= 0)
+    return 0.0f;
+
+  EnsureResources(cfg.w, cfg.h);
+  if (!m_hdcMem || !m_hbm)
+    return 0.0f;
+
+  if (!BitBlt(m_hdcMem, 0, 0, cfg.w, cfg.h, m_hdcScreen, cfg.x, cfg.y,
+              SRCCOPY)) {
+    return 0.0f;
+  }
 
-    BITMAPINFO bmi = { 0 };
-    bmi.bmiHeader.biSize = sizeof(BITMAPINFOHEADER);
-    bmi.bmiHeader.biWidth = cfg.w;
-    bmi.bmiHeader.biHeight = -cfg.h; // Top-down
-    bmi.bmiHeader.biPlanes = 1;
-    bmi.bmiHeader.biBitCount = 32;
-    bmi.bmiHeader.biCompression = BI_RGB;
+  BITMAPINFO bmi = {0};
+  bmi.bmiHeader.biSize = sizeof(BITMAPINFOHEADER);
+  bmi.bmiHeader.biWidth = cfg.w;
+  bmi.bmiHeader.biHeight = -cfg.h; // Top-down
+  bmi.bmiHeader.biPlanes = 1;
+  bmi.bmiHeader.biBitCount = 32;
+  bmi.bmiHeader.biCompression = BI_RGB;
 
-    std::vector<DWORD> pixels(cfg.w * cfg.h);
-    if (GetDIBits(m_hdcMem, m_hbm, 0, cfg.h, pixels.data(), &bmi, DIB_RGB_COLORS) == 0) {
-        return 0.0f;
-    }
+  std::vector<DWORD> pixels(cfg.w * cfg.h);
+  if (GetDIBits(m_hdcMem, m_hbm, 0, cfg.h, pixels.data(), &bmi,
+                DIB_RGB_COLORS) == 0) {
+    return 0.0f;
+  }
 
-    int match = 0;
-    BYTE tr = GetRValue(cfg.target);
-    BYTE tg = GetGValue(cfg.target);
-    BYTE tb = GetBValue(cfg.target);
+  int match = 0;
+  BYTE tr = GetRValue(cfg.target);
+  BYTE tg = GetGValue(cfg.target);
+  BYTE tb = GetBValue(cfg.target);
 
-    for (DWORD p : pixels) {
-        BYTE b = p & 0xFF;
-        BYTE g = (p >> 8) & 0xFF;
-        BYTE r = (p >> 16) & 0xFF;
+  for (DWORD p : pixels) {
+    BYTE b = p & 0xFF;
+    BYTE g = (p >> 8) & 0xFF;
+    BYTE r = (p >> 16) & 0xFF;
 
-        if (abs(r - tr) <= cfg.tolerance && 
-            abs(g - tg) <= cfg.tolerance && 
-            abs(b - tb) <= cfg.tolerance) {
-            match++;
-        }
-    }
+    if (abs(r - tr) <= cfg.tolerance && abs(g - tg) <= cfg.tolerance &&
+        abs(b - tb) <= cfg.tolerance) {
+      match++;
+    }
+  }
 
-    return (float)match / (float)(cfg.w * cfg.h);
+  return (float)match / (float)(cfg.w * cfg.h);
 }
Index: include/shared/Detector.h
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#ifndef DETECTOR_H\r\n#define DETECTOR_H\r\n\r\n#include <windows.h>\r\n#include <vector>\r\n\r\nstruct RoiConfig {\r\n    int x, y, w, h;\r\n    COLORREF target;\r\n    int tolerance;\r\n};\r\n\r\nclass FovDetector {\r\npublic:\r\n    FovDetector();\r\n    ~FovDetector();\r\n\r\n    float Scan(const RoiConfig& cfg);\r\n\r\nprivate:\r\n    HDC m_hdcScreen;\r\n    HDC m_hdcMem;\r\n    HBITMAP m_hbm;\r\n    HGDIOBJ m_hOld;\r\n    int m_curW, m_curH;\r\n\r\n    void EnsureResources(int w, int h);\r\n};\r\n\r\n#endif // DETECTOR_H\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/include/shared/Detector.h b/include/shared/Detector.h
--- a/include/shared/Detector.h	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/include/shared/Detector.h	(date 1775748098231)
@@ -1,30 +1,32 @@
 #ifndef DETECTOR_H
 #define DETECTOR_H
 
-#include <windows.h>
+#include <stdexcept>
 #include <vector>
+#include <windows.h>
+
 
 struct RoiConfig {
-    int x, y, w, h;
-    COLORREF target;
-    int tolerance;
+  int x, y, w, h;
+  COLORREF target;
+  int tolerance;
 };
 
 class FovDetector {
 public:
-    FovDetector();
-    ~FovDetector();
+  FovDetector();
+  ~FovDetector();
 
-    float Scan(const RoiConfig& cfg);
+  float Scan(const RoiConfig &cfg);
 
 private:
-    HDC m_hdcScreen;
-    HDC m_hdcMem;
-    HBITMAP m_hbm;
-    HGDIOBJ m_hOld;
-    int m_curW, m_curH;
+  HDC m_hdcScreen;
+  HDC m_hdcMem;
+  HBITMAP m_hbm;
+  HGDIOBJ m_hOld;
+  int m_curW, m_curH;
 
-    void EnsureResources(int w, int h);
+  void EnsureResources(int w, int h);
 };
 
 #endif // DETECTOR_H
Index: src/shared/Input.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"shared/Input.h\"\r\n#include <iostream>\r\n\r\nvoid RegisterRawMouse(HWND hwnd) {\r\n    RAWINPUTDEVICE rid;\r\n    rid.usUsagePage = 0x01;  // Generic Desktop\r\n    rid.usUsage = 0x02;      // Mouse\r\n    rid.dwFlags = RIDEV_INPUTSINK; \r\n    rid.hwndTarget = hwnd;\r\n\r\n    if (!RegisterRawInputDevices(&rid, 1, sizeof(rid))) {\r\n        std::cerr << \"Failed to register raw input devices.\" << std::endl;\r\n    }\r\n}\r\n\r\nint GetRawInputDeltaX(LPARAM lparam) {\r\n    UINT dwSize;\r\n    GetRawInputData((HRAWINPUT)lparam, RID_INPUT, NULL, &dwSize, sizeof(RAWINPUTHEADER));\r\n    \r\n    if (dwSize == 0) return 0;\r\n\r\n    LPBYTE lpb = new BYTE[dwSize];\r\n    if (lpb == NULL) return 0;\r\n\r\n    if (GetRawInputData((HRAWINPUT)lparam, RID_INPUT, lpb, &dwSize, sizeof(RAWINPUTHEADER)) != dwSize) {\r\n        delete[] lpb;\r\n        return 0;\r\n    }\r\n\r\n    RAWINPUT* raw = (RAWINPUT*)lpb;\r\n    int dx = 0;\r\n\r\n    if (raw->header.dwType == RIM_TYPEMOUSE) {\r\n        dx = raw->data.mouse.lLastX;\r\n    }\r\n\r\n    delete[] lpb;\r\n    return dx;\r\n}\r\n#include \"shared/State.h\"\r\n\r\nvoid PollInputKeys() {\r\n    static bool f9Pressed = false;\r\n    if (GetAsyncKeyState(VK_F9) & 0x8000) {\r\n        if (!f9Pressed) {\r\n            g_showROIBox = !g_showROIBox;\r\n            f9Pressed = true;\r\n        }\r\n    } else {\r\n        f9Pressed = false;\r\n    }\r\n\r\n    static bool rPressed = false;\r\n    if ((GetAsyncKeyState(VK_CONTROL) & 0x8000) && (GetAsyncKeyState('R') & 0x8000)) {\r\n        if (!rPressed) {\r\n            if (g_currentSelection == NONE) {\r\n                g_currentSelection = SELECTING_ROI;\r\n                g_isSelectionActive = true;\r\n            } else {\r\n                g_currentSelection = NONE;\r\n                g_isSelectionActive = false;\r\n            }\r\n            rPressed = true;\r\n        }\r\n    } else {\r\n        rPressed = false;\r\n    }\r\n}\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/shared/Input.cpp b/src/shared/Input.cpp
--- a/src/shared/Input.cpp	(revision 32e07b5da1e28325ef5c527aedc786f277811e28)
+++ b/src/shared/Input.cpp	(date 1775748098234)
@@ -2,67 +2,74 @@
 #include <iostream>
 
 void RegisterRawMouse(HWND hwnd) {
-    RAWINPUTDEVICE rid;
-    rid.usUsagePage = 0x01;  // Generic Desktop
-    rid.usUsage = 0x02;      // Mouse
-    rid.dwFlags = RIDEV_INPUTSINK; 
-    rid.hwndTarget = hwnd;
+  RAWINPUTDEVICE rid;
+  rid.usUsagePage = 0x01; // Generic Desktop
+  rid.usUsage = 0x02;     // Mouse
+  rid.dwFlags = RIDEV_INPUTSINK;
+  rid.hwndTarget = hwnd;
 
-    if (!RegisterRawInputDevices(&rid, 1, sizeof(rid))) {
-        std::cerr << "Failed to register raw input devices." << std::endl;
-    }
+  if (!RegisterRawInputDevices(&rid, 1, sizeof(rid))) {
+    std::cerr << "Failed to register raw input devices." << std::endl;
+  }
 }
 
 int GetRawInputDeltaX(LPARAM lparam) {
-    UINT dwSize;
-    GetRawInputData((HRAWINPUT)lparam, RID_INPUT, NULL, &dwSize, sizeof(RAWINPUTHEADER));
-    
-    if (dwSize == 0) return 0;
+  UINT dwSize;
+  GetRawInputData((HRAWINPUT)lparam, RID_INPUT, NULL, &dwSize,
+                  sizeof(RAWINPUTHEADER));
+
+  if (dwSize == 0)
+    return 0;
 
-    LPBYTE lpb = new BYTE[dwSize];
-    if (lpb == NULL) return 0;
+  LPBYTE lpb = new BYTE[dwSize];
+  if (lpb == NULL)
+    return 0;
 
-    if (GetRawInputData((HRAWINPUT)lparam, RID_INPUT, lpb, &dwSize, sizeof(RAWINPUTHEADER)) != dwSize) {
-        delete[] lpb;
-        return 0;
-    }
+  if (GetRawInputData((HRAWINPUT)lparam, RID_INPUT, lpb, &dwSize,
+                      sizeof(RAWINPUTHEADER)) != dwSize) {
+    delete[] lpb;
+    return 0;
+  }
 
-    RAWINPUT* raw = (RAWINPUT*)lpb;
-    int dx = 0;
+  RAWINPUT *raw = (RAWINPUT *)lpb;
+  int dx = 0;
 
-    if (raw->header.dwType == RIM_TYPEMOUSE) {
-        dx = raw->data.mouse.lLastX;
-    }
+  if (raw->header.dwType == RIM_TYPEMOUSE) {
+    dx = raw->data.mouse.lLastX;
+  }
 
-    delete[] lpb;
-    return dx;
+  delete[] lpb;
+  return dx;
 }
 #include "shared/State.h"
+#include <mutex>
 
 void PollInputKeys() {
-    static bool f9Pressed = false;
-    if (GetAsyncKeyState(VK_F9) & 0x8000) {
-        if (!f9Pressed) {
-            g_showROIBox = !g_showROIBox;
-            f9Pressed = true;
-        }
-    } else {
-        f9Pressed = false;
-    }
+  std::lock_guard<std::mutex> lock(g_stateMutex);
+  static bool f9Pressed = false;
+  if (GetAsyncKeyState(VK_F9) & 0x8000) {
+    if (!f9Pressed) {
+      g_showROIBox = !g_showROIBox;
+      f9Pressed = true;
+    }
+  } else {
+    f9Pressed = false;
+  }
 
-    static bool rPressed = false;
-    if ((GetAsyncKeyState(VK_CONTROL) & 0x8000) && (GetAsyncKeyState('R') & 0x8000)) {
-        if (!rPressed) {
-            if (g_currentSelection == NONE) {
-                g_currentSelection = SELECTING_ROI;
-                g_isSelectionActive = true;
-            } else {
-                g_currentSelection = NONE;
-                g_isSelectionActive = false;
-            }
-            rPressed = true;
-        }
-    } else {
-        rPressed = false;
-    }
+  static bool rPressed = false;
+  if ((GetAsyncKeyState(VK_CONTROL) & 0x8000) &&
+      (GetAsyncKeyState('R') & 0x8000)) {
+    if (!rPressed) {
+      if (g_currentSelection == NONE) {
+        g_currentSelection = SELECTING_ROI;
+        g_isSelectionActive = true;
+      } else {
+        g_currentSelection = NONE;
+        g_isSelectionActive = false;
+      }
+      rPressed = true;
+    }
+  } else {
+    rPressed = false;
+  }
 }
